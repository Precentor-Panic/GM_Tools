# GM Tools — Reusable Stack Plan

A collection of tools for LLM-assisted tabletop RPG session design and live play. Generalized from "The Threadbare City" one-shot (Merrath, pinned at `/home/russell/campaign/`). Not D&D-specific; applicable to any TTRPG that uses World Fabric for world state.

---

## The Core Pattern

Any sufficiently complex one-shot or campaign session can be modeled as:

```
event fires at a graph node
  → BFS outward: (node_id, distance, path_edge_type)[]
  → LLM call [mutation]: event semantics × blast radius → structured graph mutations
  → apply mutations to graph
  → LLM call [scene]: updated subgraph snapshot → scene/consequence description (2–3 paragraphs)
```

This "blast radius" pattern works for any world-as-graph system. The tools in this collection implement it in a config-driven, session-agnostic way.

---

## LLM vs. Non-LLM Split

| Task | LLM | Reason |
|------|-----|--------|
| BFS blast radius traversal | ❌ | Pure graph math |
| Apply graph mutations to WF | ❌ | Pure data write |
| Doom clock counter / threshold | ❌ | Simple integer |
| WFI import / export | ❌ | Existing code |
| Scene / actor creation in Foundry | ❌ | API call |
| Event mutation interpretation | ✅ | Semantic + creative |
| Scene-setting narration | ✅ | Creative, contextual |
| Map prompt generation | ✅ | One-time design task |
| NPC card generation | ✅ | One-time design task |

The LLM is called twice per event — once to decide what changes (structured output only), once to describe the result (bounded narration). Everything else is deterministic code.

---

## Tool 1: World Fabric "Graph Push to Foundry" (Feature Request)

**This belongs in the World Fabric project, not this folder. File as a GitHub issue / CLAUDE.md note there.**

**Location:** `/home/russell/foundry_worldFabric`  
**Integration point:** `scripts/apps/cockpit-app.mjs` lines 645–662, `_createFoundryDoc`  
**Current state:** Creates one entity at a time via right-click UI  

**Feature scope:**
- Add "Push to Foundry" bulk action (toolbar or multi-select right-click)
- For each selected entity: `place` → Foundry Scene, `person` → Actor, `faction` → JournalEntry
- Seed fields: `name`, `description`, `imageUrl` (as scene background or actor image), `tags` as Foundry flags
- After creation: write Foundry doc UUID back to entity's `foundryRef` field (already supported in schema)
- Scenes: `description` → scene notes; movement edges → scene navigation links (if Foundry v14 supports)

**Not built here.** Write the feature request and leave a pointer from this file.

---

## Tool 2: Foundry MCP Server (Install, Don't Build)

Install from Foundry package manager into local Foundry instance (`http://localhost:30000`). Configure in Claude Code MCP settings.

**Options (in order of preference):**
1. `foundry-mcp-bridge` — official Foundry package, package manager install
2. `laurigates/foundryvtt-mcp` — 20 tools: actor querying, scene management, content generation, world search
3. `adambdooley/foundry-vtt-mcp` — bridges Foundry data with Claude Desktop

**Verification:** After install, confirm Claude can query actors and scenes via MCP tool calls.

**Not built here.** Install action only.

---

## Tool 3: World Fabric MCP Server

**Location:** `GM_Tools/wf-mcp-server/`  
**Why:** Querying a full WF graph serialized into LLM context is expensive and drifts. An MCP server lets Claude make targeted queries during live sessions without owning state.

**Operations:**

| Operation | Input | Output |
|-----------|-------|--------|
| `get_entity(id)` | Entity ID | Single entity with all attributes |
| `get_adjacent(id, depth)` | Entity ID + hop count | Subgraph within N hops (blast radius input) |
| `apply_mutations(mutations[])` | Array of WF schema mutations | Calls `importGraph` from `interchange.mjs`, returns updated entity/edge counts |
| `get_entities_by_type(type)` | Entity type string | All entities of that type |
| `get_session_state()` | — | Current doom clock position, thread statuses (from session-layer bookkeeping) |

**Implementation:** Wraps `GraphService` (`scripts/data/graph-service.mjs`) via Foundry's module API. If running outside Foundry context, reads from exported WFI JSON directly.

**WFI mutation schema (output format for `apply_mutations`):**
```json
{
  "version": "1.0",
  "entities": [
    { "id": "...", "attributes": { "key": "newValue" } }
  ],
  "edges": [
    { "sourceId": "...", "targetId": "...", "strength": 0.8, "valence": "negative" }
  ]
}
```

---

## Tool 4: Parameterizable Session Runner

**Location:** `GM_Tools/session-runner/`  
**Why:** The blast-radius pipeline doesn't need to be baked into any specific campaign. Any session config + WFI graph → runnable session.

**Config schema:**
```json
{
  "session": {
    "name": "",
    "doomClockThreshold": 7,
    "doomClockMode": "scene-counter"
  },
  "events": {
    "scripted": [
      { "id": "T-1", "type": "scripted", "narrativePath": "narrations/t1.md" },
      { "id": "penultimate", "type": "scripted", "trigger": "doom_clock_threshold" }
    ],
    "pool": [
      {
        "id": "A",
        "tier": "linen",
        "phrase": "...",
        "type": "dynamic"
      }
    ]
  },
  "pipeline": {
    "bfsDepth": 3,
    "edgeWeights": { "causal": 1.0, "fealty": 0.9, "kinship": 0.85, "membership": 0.7, "ownership": 0.6, "containment": 0.6, "origin": 0.6, "presence": 0.55, "knowledge": 0.5, "social": 0.4, "unspecified": 0.3 },
    "textureModel": "claude-sonnet-5",
    "textureModelOverride": "claude-opus-4-8",
    "sceneModel": "claude-sonnet-5"
  }
}
```
*(`edgeWeights` now covers all of World Fabric's `RELATIONSHIP_TYPES` — the propagation engine in `mutation-engine/propagate.mjs` needs a weight for every type it might encounter. `mutationModel`/`sceneModel` renamed `textureModel`/`sceneModel` to match the phased plan below: the mutation/texturing pass is now the propagation engine's batched LLM call, not a monolithic session-runner call. Phase 1.5 split World Fabric's old catch-all `location` type into `containment` (structural, e.g. a place's parent region — excluded from `ambientDecay` entirely) and `presence` (temporal, e.g. an actor's recent whereabouts — decays same as `location` did); `edgeWeights` above reflects the rename. Phase 1.5b resolved the `person.homeLocation` question left open by Phase 1.5: it's `origin` (biographical — where someone is *from*, distinct from `containment`'s structural sense even though both are hard-excluded from `ambientDecay`), weighted the same as `containment`. See `plans/phase-1.5-tasks.md`.)*

**Components:**

- `burn-algorithm.mjs` — BFS traversal of WF graph from event epicenter. Returns `(node_id, distance, path_edge_types)[]`. Uses edge weight config for severity scoring.
- `apply-mutations.mjs` — Takes structured LLM mutation output, writes to World Fabric via `apply_mutations` MCP call (or direct WFI import in merge mode).
- `session-runner.mjs` — Orchestrator. Manages doom clock counter, routes events to scripted or pipeline path, sequences the two LLM calls, triggers penultimate event at threshold.
- `prompt-mutation.md` — LLM Call 1 template. Input: event phrase/type + blast radius list + node states. Output: JSON mutations in WF schema. No free text.
- `prompt-scene.md` — LLM Call 2 template. Input: current location, reachable areas, updated subgraph diff. Output: 2–3 paragraphs ending on sensory hook or decision point. No character voice, no state tracking.

**Doom clock modes:**
- `scene-counter` — increments per scene completed. Threshold fires penultimate. Bidirectional (AW-style): event can advance clock OR clock advancing can fire event.
- `real-time` — wall-clock based fallback for firing scripted endings at thematically appropriate moments (optional, session-specific config).

---

## Tool 5: Claude Code Skills

**Location:** `~/.claude/CLAUDE.md` (global) or project-level CLAUDE.md  
**Why:** Structured input → consistent output. Reduces planning drift. Repeatable design tasks that shouldn't cost open-ended conversation tokens.

| Skill | Input | Output |
|-------|-------|--------|
| `/plan-session [doc]` | Session design .md | WFI seed JSON + NPC cards + event pool config |
| `/npc-card [name] [role] [secret] [motivation]` | Structured fields | Formatted NPC entity ready for WFI import |
| `/scene-brief [location] [event] [party]` | Structured context | 2–3 paragraph scene description + suggested event/thread |
| `/event-resolve [phrase] [target] [nearby-nodes]` | Structured input | Structured graph mutations (WF schema) |
| `/push-session [config]` | Session config file | Validates config, confirms pool, previews doom clock, confirms BFS depth |

Each skill is a CLAUDE.md-defined prompt template with explicit field structure. Not open-ended conversation.

---

## Maps Workflow

No API path exists for contextual, artistically appropriate map generation from a node graph. Workflow:

**Step 1 — Generate map prompts (LLM, one-time per district/area):**  
Produce a detailed text prompt per district and key area-node. Prompt specifies: city style, district character, landmarks, mood, visual references. Save to `campaign/maps/prompts/[district]-[area-node].md`.

**Step 2 — Generate maps (manual, any tool):**  
Take prompts to GPT-4o, Gemini, Midjourney, commissioned artist, or commercial map pack. Output to `campaign/maps/generated/[district]-[area-node].png`.

**Commercial packs to evaluate (Foundry-native):** Baileywiki, 2-Minute Tabletop, Tom Cartos (urban focus). A city pack likely covers all districts without custom generation.

**Step 3 — Verify (Claude vision):**  
Claude can read map images via vision. Confirm each map matches its area-node description before assigning.

**Step 4 — Graph Push loads them:**  
Tool 1 (Graph Push feature) reads `imageUrl` from WF entities and assigns maps as Foundry scene backgrounds during bulk push. Filename convention: `[district]-[area-node].png` must match WF entity names.

---

## Build Sequence (steps 1–3: historical, still accurate) / Phased Toolbox Plan (current)

Steps 1–3 below predate the toolbox reframing and remain accurate completed-work history. **From step 4 onward, this linear sequence is superseded** by a phased, independently-shippable toolbox plan — see `plans/phase-N-tasks.md` for the current, detailed execution checklists.

| Step | Item | Location | Notes |
|------|------|----------|-------|
| 1 | ~~File~~ Build Graph Push | World Fabric project | Built directly (M13a), not just filed. `cockpit-app.mjs _pushSelectionToFoundry()` |
| 2 | Install Foundry MCP server | Foundry package manager | Configured in `~/.mcp.json` as `foundryvtt` (generic actor/scene MCP) — pending approval in Claude Code |
| 3 | Build WF MCP server | `GM_Tools/wf-mcp-server/` | **Done.** Reads `world-fabric-snapshot.json` / writes `world-fabric-mutations.json` (the file bridge GraphService already exports) rather than reaching into the Foundry module directly — GraphService only runs in-browser. Registered in `~/.mcp.json` as `world-fabric`. Smoke-tested against live `wf-test` snapshot. |

**Current phased plan** (toolbox framing — each phase independently shippable, no single critical path; see `plans/` for task-level detail):

| Phase | Deliverable | Ships independently? | Status |
|---|---|---|---|
| 0 | Prep-time tools (NPC cards, scene briefs, doom clocks, VTT push via step 1's Graph Push) | Yes | Not started |
| 1 | Mutation engine core: schema, propagation pass, review-state, diff, grain, rollback, batched texturing, conversational MCP review tools | Partially (infra) | **Done** — tasks 1.1–1.8 built and unit-tested (`mutation-engine/`, 7 new `wf-mcp-server` tools); task 1.9 (dedicated web review UI) explicitly deferred to Phase 6 per its own text. See `plans/phase-1-tasks.md` and `mutation-engine/README.md`. Propagation-tuning constants (task 1.3) are prototype defaults, calibration deferred. Post-Phase-1 remediation also landed: `Mutation`/`StoredMutation` schema split (dropped `.passthrough()`), `diff.mjs` wired into the live review path, `npm test` fixed. |
| 1.5 | **World Fabric containment/presence split** — fixes a real schema gap (see `plans/phase-1.5-tasks.md`): `location` was conflating structural containment with decaying presence, which would have made GM_Tools' own `ambientDecay` eventually produce spurious "building stopped being in its district" mutations on a long campaign. Out of numeric order, inserted before Phase 2 because Phase 2's `contained-in` scope mode depends on it. | Yes (fixes existing behavior) | **Done** — tasks 1.5.1–1.5.4 landed across both repos: World Fabric gained `containment`/`presence` relationship types (`place.region`/`faction.headquarters` derive `containment`; world-scan's token-placement tracking uses `presence`; one-time migration relabels persisted `scan:loc:*` edges), GM_Tools' `ambientDecay` hard-excludes `containment` while `propagateSeed` still traverses it normally. **1.5b (done):** `person.homeLocation` resolved to its own type, `origin` (biographical — where someone is *from*; never decays, but isn't structural containment either) rather than reusing `containment` or moving to `presence`. Needed its own entityTypes-level migration in World Fabric (`_runMigration`'s "merge new attributeDefs" step is additive-only and does not retroactively update an already-persisted attributeDef's `deriveEdge`, contrary to Phase 1.5's original "no migration needed" assumption — confirmed by inspection against a live test world). GM_Tools' `ambientDecay` exclusion refactored to a `NON_DECAYING_RELATIONSHIP_TYPES` set covering both `containment` and `origin`; `propagateSeed` traverses `origin` normally. **1.5c (done):** found and fixed the same additive-only-migration gap for the *original* `containment` case — `place.region`/`faction.headquarters`'s persisted `deriveEdge` was still frozen at `location` on already-existing worlds (confirmed live against `wf-test`) despite `constants.mjs` saying `containment` since 1.5. Migration extended to cover both; live-reverified. |
| 2/2b | Time-skip mode + headless (Foundry-optional) apply — the MVP wedge, now including LLM-inferred seed resolution and `region`/`contained-in` scope modes | Yes | **Done** — tasks 2.0–2.4 built and tested per `plans/phase-2-tasks.md`. `time-skip/scope.mjs`/`run.mjs` extracted from `wf_propose_mutations`'s prior inline logic (byte-identical, task 2.0); `region` (shallow, all-edge-type BFS) and `contained-in` (containment-typed-edges-only reachability, unbounded-ish depth) scope modes added (task 2.1); `orchestrateBatch` gained per-region checkpointing/resumability with a real cross-process kill-and-resume test (task 2.2); `time-skip/resolve-seed.mjs` resolves a freeform event description to an entity id via LLM, kept deliberately separate from the deterministic `resolveScope` (task 2.2b); `graph-import/headless-apply.mjs` applies mutations directly to a standalone snapshot with no live Foundry client, reusing `interchange.mjs`'s `importGraph` (task 2.3); `wf_sync_to_foundry` now falls back to the headless path and always reports which path was used (task 2.4). A remediation pass found and fixed two real issues: `texture.mjs`/`resolve-seed.mjs`'s duplicated LLM-call plumbing extracted to `mutation-engine/llm-call.mjs`, and `contained-in` + ambient mode was a structural no-op (containment edges — used to find the in-scope entity set — are themselves excluded from decay; fixed to decay any real edge between two already-in-scope entities instead). The two real-API integration smoke tests (2.2's cost-reduction check, 2.2b's resolveSeed check) are written and unit-tested via mocks but **not executed** — `ANTHROPIC_API_KEY` was not set in the build environment. |
| 3 | Live on-demand diff mode | Yes | **Done** — tasks 3.1–3.3 built and tested per `plans/phase-3-tasks.md`. Task 3.1 confirmed `time-skip/scope.mjs`'s existing `seed` mode needed no code changes, but measured (5 real timed runs each, depth=2 and depth=3, against a 50-entity/129-edge fixture) that `wf_propose_mutations` is NOT reliably sub-5s live: depth=3 p50=59.0s (min 34.4s, max 59.9s), depth=2 p50=51.4s (min 26.1s, max 62.5s) — depth barely moves the number, since the real cost driver is texture-eligible entity count within the single affected region (-> output tokens for that one API call), not BFS hop count, so `scope.mjs`'s depth default was deliberately left unchanged at 3. Task 3.2 built `mutation-engine/narrate.mjs` — the project's long-missing second LLM call (`PLAN.md`'s original "Core Pattern": mutation call + narration call) — producing 2-3 paragraphs of player-facing prose per accepted batch, hard-gated so it never runs against a batch with any non-`accepted` mutation (`NarrationGateError`, code-enforced, not just prompt-level), reusing `llm-call.mjs`'s shared plumbing. Task 3.3 wired `wf_narrate_batch` into `wf-mcp-server/index.mjs` and added a latency-guidance note (citing 3.1's real numbers) to `wf_propose_mutations`'s tool description so a calling Claude Code session says "resolving, one moment" before a live-diff proposal rather than appearing to hang. A real-subprocess, real-MCP-protocol, real-API round trip (propose → review → accept → narrate → regenerate-with-note → sync, plus confirming the narration gate refuses a batch with pending/rejected mutations) ran and passed against the live Anthropic API — see `wf-mcp-server/test/live-diff-narrate.smoke.mjs` and the Phase 3 closing report for the actual transcript/narration text. |
| 3.5 | **Deferred/lazy consequence resolution** — for large time-skips, resolve only a per-cycle "headline" scope eagerly (reusing Phase 3's `seed`-mode mechanism) and record everything else as a cheap, unresolved ledger tag instead of texturing or discarding it. Zero further cost unless a tagged entity is later explicitly asked about; on-demand resolution caps fan-out by impact score (top-K, not sub-clustering) rather than risking a cost spike on a high-degree anchor. | Yes | **Done** — tasks 3.5.1–3.5.5 built and tested per `plans/phase-3.5-tasks.md`. `mutation-engine/pending-ledger.mjs` (task 3.5.1) is the new per-entity JSON ledger (`pending-resolution/<world>/<entityId>.json`), reusing `review-state.mjs`'s `withLock`/`ConcurrentWriteError` (exported for that purpose) rather than duplicating file-locking; schema.mjs bumped to `SCHEMA_VERSION` 2 for the additive `SourceKind` value `'deferred-resolution'` and `Batch.resolvedPendingEntries`. `time-skip/run-cycle.mjs`'s `orchestrateCycle` (task 3.5.2, new file, `orchestrateBatch` untouched) textures one small headline neighborhood per cycle and defers everything else in the cycle's scope to the ledger — including deltas that would have cleared `needsLLM` on their own — plus a growth-bound sweep that folds an already-bloated entity's full backlog into the current cycle rather than letting it grow unbounded. `time-skip/resolve-pending.mjs`'s `resolvePending` (task 3.5.3) is the explicit on-demand resolve: gathers a requested entity's own backlog plus its BFS neighborhood's pending-bearing entities, applies **the fan-out cap** (top-`maxNeighbors`, default 8, by impact score — flat, not sub-clustering, per the architect's settled reasoning), and renders everything **chronologically by `cycleDescriptor`** (new `renderPendingResolutionSummary` in `texture.mjs`, reusing `textureRegion` via a new `deltaSummaryOverride` hook rather than forking a parallel texturing path) in one API call. Task 3.5.4 wires `pending-ledger.mjs`'s `applyLedgerOutcome` into `wf_accept`/`wf_reject` — accept clears resolved entries, reject reverts them to `'pending'`, never silently dropped. Task 3.5.5 adds `wf_run_cycle`/`wf_resolve_pending` to `wf-mcp-server/index.mjs`; confirmed by direct inspection that none of `wf_get_entity`/`wf_get_context`/`wf_get_adjacent`'s handlers reference the resolve path. A real, API-backed multi-cycle round trip (`wf_run_cycle` ×3 with rotating headline foci → backlog accumulation confirmed → `wf_resolve_pending` → real LLM output that explicitly synthesizes all three cycles → `wf_accept` → ledger cleared → `wf_sync_to_foundry` → a 4th cycle → resolve again → `wf_reject` → entry back to `'pending'`, not gone → `wf_rollback_batch` still functions on a resolve-originated batch) ran twice against the live Anthropic API and passed both times — see `wf-mcp-server/test/deferred-resolution-multicycle.smoke.mjs`. A self-review remediation pass found and fixed one real issue (a `safeBatchHeadline` helper and a `readPending(...).filter(status==='pending')` idiom duplicated verbatim across `run-cycle.mjs`/`resolve-pending.mjs`, factored into `pending-ledger.mjs` as `sourceBatchHeadline`/`readAvailablePending`); the fan-out cap this phase builds is noted as the same mechanism that would fix Phase 3's live-diff latency (~50s p50) if that's ever revisited, but is deliberately NOT retrofitted into that path as part of this phase. |
| 4 | Rollback hardening + unreviewed-accumulation tracking | Yes | **Done** — tasks 4.1–4.2 built and tested per `plans/phase-4-tasks.md`. Task 4.1 closed the confirmed newly-created-entity rollback gap: `graph-import/headless-apply.mjs`'s `applyHeadless` now pre-assigns and reports back the id it gives every id-less create (same `wf_<ts>_<n>` convention `interchange.mjs`'s own generator uses, one shared counter so the two can never collide), and `wf_sync_to_foundry` writes that id back onto the batch's stored mutation entry so `rollback.mjs`'s existing preState:null + entry.id logic resolves to a real delete instead of skipping — no code change needed in `rollback.mjs` itself. The live-Foundry path has the same underlying gap but is confirmed unclosable from this side without a `foundry_worldFabric` change (the mutation watcher never reports a created id back through the file bridge) — documented, not silently left unexamined, and still gracefully skipped rather than crashing. A real end-to-end proof (`wf-mcp-server/test/rollback-created-entity.test.mjs`, spawns the real MCP server against a real fixture snapshot) confirmed a created entity is genuinely deleted from the snapshot after propose→accept→sync(headless)→rollback. Found and fixed one adjacent gap while proving this: `wf_rollback_batch` had no headless fallback at all (unlike `wf_sync_to_foundry`), so a rollback could never actually apply against a genuinely headless-only campaign — fixed by extracting the shared live-then-headless behavior into `applyMutationsWithHeadlessFallback()`, reused by both tools; reverified against the real, API-backed `deferred-resolution-multicycle.smoke.mjs`. Task 4.2 built unreviewed-accumulation tracking from scratch: new `mutation-engine/human-review.mjs` tracks per-entity `lastHumanReviewedAt`/`unreviewedAcceptCount`, updated by `wf_accept`/`wf_reject`/`wf_regenerate` at `region`/`entity` scope and `wf_review_batch` at `region`/`entity` grain, but deliberately NOT by a whole-`batch`-scope accept-all (the entire point of the feature) or `headline`-grain viewing (a collapsed overview, not a shown diff); new `wf_get_unreviewed_entities` tool surfaces flagged entities (never-reviewed / stale-by-`maxAgeDays` / accumulated-by-`maxUnreviewedAccepts`, both configurable, substituting "N days" for the original "N sessions" framing since no per-entity session-counter primitive exists elsewhere in this project); `grain.mjs`'s `summarizeBatch` gained an optional `flaggedEntityIds` Set (stays pure/Foundry-free) that forces a flagged entity out of headline collapse regardless of importance. A real end-to-end MCP-protocol test (`wf-mcp-server/test/unreviewed-accumulation.test.mjs`) proved the core distinction (batch-accept-all leaves an entity flagged; a scoped accept/entity-grain view clears it; headline-grain viewing does not) and the force-into-headline behavior. A self-review remediation pass found and fixed one real issue (a repo-pollution bug: four existing wf-mcp-server test/smoke files exercising `wf_accept` through the real MCP server weren't isolating `GM_TOOLS_HUMAN_REVIEW_DIR`, so running them wrote real files into the repo's default `human-review/` root — fixed to match the established `GM_TOOLS_REVIEW_STATE_DIR`/`GM_TOOLS_PENDING_LEDGER_DIR` isolation pattern) plus two small non-functional cleanups in `human-review.mjs` (a duplicated per-entity lookup, a redundant `Date` round-trip).
| 5 | Import-from-writeup | Yes | **Done** — tasks 5.1–5.3 built and tested per `plans/phase-5-tasks.md`. Task 5.1 (`graph-import/writeup-import.mjs`'s `proposeWfiFromWriteup`, `prompts/writeup-import.md`) is one LLM call turning freeform text into a WFI-shaped proposal (entities/edges referencing endpoints by name, never an id, each with a `rationale`), reusing `mutation-engine/llm-call.mjs`'s shared plumbing and the same retry-once-then-typed-error convention as `texture.mjs`/`resolve-seed.mjs`, gated by a simple v1 length guard (`MAX_WRITEUP_CHARS`, a typed `WriteupTooLargeError`). Task 5.2 (`previewWriteupImport`, same file) dry-runs the proposal through `interchange.mjs`'s `importGraph` WITHOUT persisting and converts the effective changes into a normal review-state.mjs batch (`importWriteup`) — confirmed by a real test (not assumed) that a writeup mentioning both a brand-new entity and one already in the graph produces one create and one update targeting the real existing id, not two creates; edges referencing entities purely by name resolve correctly and stub-creation happens for a referenced-but-undescribed endpoint, matching `importGraph`'s existing behavior exactly. New additive `SourceKind` value `'writeup-import'` (`schema.mjs`, `SCHEMA_VERSION` 2 → 3). `regenerateWriteupImport` re-invokes the LLM call against the batch's own recorded source text (`batch.scope.text`) plus a steering note, replacing the whole batch's mutations — writeup extraction is a holistic pass, not a per-entity delta, so `wf_regenerate`'s `scope='entity'` is refused for a writeup-import batch with a clear typed error rather than attempting an unsound partial replace. Task 5.3 wired `wf_propose_from_writeup` into `wf-mcp-server/index.mjs` as a thin wrapper; `wf_accept`/`wf_reject`/`wf_sync_to_foundry`/`wf_rollback_batch` needed NO changes at all (already generic over batch shape), and `wf_regenerate` gained the dispatch described above. A real, API-backed MCP-protocol round trip (`wf-mcp-server/test/writeup-import-roundtrip.smoke.mjs`: propose → review → regenerate-with-note → accept → sync) ran against BOTH an existing populated snapshot (confirming an existing entity deduped to exactly one, not duplicated) AND a freshly-bootstrapped empty snapshot (confirming the bootstrap-from-nothing case genuinely works, not assumed from Phase 2.3's original scope) — both passed. That same smoke test caught a real bug before it shipped: every other mutation producer in this codebase omits `id` on a genuine create and lets it be assigned later at actual apply time, safe because their deltas never need one create referenced by another mutation in the same batch — writeup-import breaks that assumption (a new entity + an edge to it, in one proposal), so leaving create ids unassigned meant the entity and the edge referencing it got assigned different ids independently at apply time, leaving the edge dangling and causing `importGraph` to spawn a phantom stub entity named after the stale id string. Fixed by having `previewWriteupImport` pre-assign and commit to real, final ids for every entity it creates, reused consistently by that entity's own upsert and any edge referencing it — regression-tested at both the unit and real-API smoke-test level. A self-review remediation pass found no other issues (data-dir isolation and no-premature-persistence were both checked and clean); it did add a short Phase 5 section to `wf-mcp-server/README.md`'s tool table. |
| 6 | Dedicated web review UI | Yes | **Done** — tasks 6.1–6.6 built and tested per `plans/phase-6-tasks.md`; design followed from `plans/phase-6-review.md` as written, no redesign. Prep (not its own numbered task, but real work found necessary before 6.1): `wf-mcp-server/index.mjs`'s wf_accept/wf_reject/wf_regenerate/wf_sync_to_foundry/wf_rollback_batch/wf_review_batch handlers carried real orchestration logic inline — extracted verbatim into new `wf-mcp-server/lib/mutation-ops.mjs` (`acceptOp`/`rejectOp`/`regenerateOp`/`narrateOp`/`syncOp`/`rollbackOp`/`reviewGrainOp` plus the dual-path-apply/id-write-back/scope-resolution helpers) and `wf-mcp-server/lib/resolve.mjs`, so review-ui reuses the EXACT code wf-mcp-server's tools call rather than re-deriving it — confirmed byte-identical behavior by rerunning wf-mcp-server's full test suite unchanged after the extraction. Also generalized accept/reject's review-marking from "batch scope never reviewed, region/entity scope always reviewed" to an explicit per-mutation `reviewedMutationIds` split (`acceptMutationIds`/`rejectMutationIds`), needed because review-ui's checkbox multi-select can genuinely mix individually-expanded and merely-bulk-selected mutations in one call — `acceptOp`/`rejectOp` are unchanged-behavior wrappers over these. Task 6.1 built `review-ui/server.mjs` (bare `node:http`, no framework, zero dependencies of its own — documented reasoning in the file) with routes for listing/detail/accept/reject/bulk-accept/bulk-reject/regenerate/narrate/sync/rollback/unreviewed-entities/pending-entities/resolve, all calling straight into `mutation-ops.mjs` and the existing library modules; a 14-case deterministic route suite (`review-ui/test/routes.test.mjs`) plus a real-API smoke test (`review-ui/test/routes-live.smoke.mjs`, run against the live Anthropic API and passed) cover every route including both narration-gate failure cases and the actual narrate/resolve-pending success paths. Tasks 6.2–6.6 built the vanilla HTML/CSS/JS frontend (`review-ui/public/`) exactly per the design doc: hash-routed single shell, four sections, sticky Accept/Reject/Select-All-Boring bar, two-column diff tables, regenerate-with-note inline, the amber accumulation-flag treatment, Deferred Debt search-and-resolve-in-place (reusing the same diff renderer, not a second one), Settings sync-status/rollback/graph-stub, an 8s undo toast, and j/k/space/Enter keyboard triage. **Real visual verification was actually performed**, not just reasoned from source: no interactive `claude-in-chrome` browser was available in this execution environment (no live user-facing Chrome instance), so a real headless Chromium was driven instead via Playwright already installed in the sibling `foundry_worldFabric` project's `node_modules` (read-only use, nothing in that repo touched) against a real running instance of `review-ui/server.mjs` pointed at hand-built fixture worlds, with actual screenshots captured and inspected. This caught a real bug no test or code-read had caught: the in-place refresh after accept/reject/regenerate/narrate was rebuilding every row from scratch and collapsing all `<details>`, including the one just acted on, so narration/regenerate results never stayed visible — fixed with an explicit open-row-preservation pass (`currentlyOpenMutationIds`/`openEntityIdHint`). Screenshots confirmed, in both light and dark mode, that the narration card (serif, parchment/warm-dark-brown tint) is visually distinct from the rationale card (sans-serif, plain neutral) at a glance, that the amber accumulation-flag border/dot/note render correctly, that keyboard j/k/space/Enter and Select-All-Boring behave correctly against real mixed-importance data, and that Deferred Debt's resolve-in-place produces a real LLM-synthesized diff. A self-review remediation pass (per the task file's own instruction to specifically re-check narration-gating and the accept-vs-review distinction) found and fixed one real gap: `acceptMutationIds`/`rejectMutationIds`'s documented default (an omitted `reviewedMutationIds` counts everything as reviewed — correct for `acceptOp`/`rejectOp`'s always-explicit callers) was passed through unchanged at review-ui's bulk-accept/bulk-reject HTTP boundary, where the safe default should be the opposite (nothing reviewed unless explicitly listed) — fixed at the route layer (not by changing the shared library default, which remains correct for its existing callers) and covered by a new regression test. One noted, deliberate design-doc reconciliation: the doc describes narration as appearing per-row, replacing "that row's action bar," but the actual backend (`narrate.mjs`) narrates a whole BATCH in one call, not one mutation — resolved by fetching the batch-level prose once and rendering it into every accepted row's own action area, satisfying the doc's visual/adjacency description without conflicting with the real API shape; documented inline in `app.js`. |
| 7 | Foundry-optional visual graph view — rescoped per direct user feedback (see Phase 10's context): not just an occasional "explore the graph" utility, but two connected surfaces — a List/Graph toggle on the Batch Review screen (context-scoped: batch entities + one-hop persisted neighbors) and a standalone whole-graph nav view (filtered to unreviewed/deferred-debt entities by default) — directly addressing the Batch Review list's "overwhelming" feeling for large writeup-import batches while color-coding status. Queue (list) stays the default landing view. | Yes | **Done** — tasks 7.1–7.4 built and tested per `plans/phase-7-tasks.md`; design followed from `plans/phase-7-review.md` as written, no redesign. Task 7.1 added `GET /api/graph` to `review-ui/server.mjs`: a thin composition over existing primitives only (`wf-mcp-server/lib/graph.mjs`'s `neighborhood()` for BFS, `mutation-ops.mjs`'s `flaggedEntityIdSet`, `pending-ledger.mjs`'s `listPendingEntities`), never a second traversal/status implementation. Batch-scoped mode (`?batchId=&depth=`) returns this batch's own proposed entities/edges (regardless of accept/reject/pending status) plus their persisted one-hop neighbors — a create with no persisted counterpart yet (texture.mjs's LLM-authored creates may omit `id` entirely) renders as a synthetic node keyed `new:<mutationId>`. Standalone mode (`?filter=`) defaults, when the param is omitted, to unreviewed-OR-deferred-debt — `filter=all` is the explicit "show everything" escape hatch. 8 new route tests confirm batch-scope excludes a two-hop-away and a fully-disconnected entity, a node can carry both status channels at once, and the default filter genuinely returns fewer nodes than `filter=all` from the real response (not a client-side hide). Task 7.2 built `review-ui/public/graph-view.js`, a single shared SVG rendering module (no charting/graph-viz library, matching this project's zero-build-step convention): circles sized by degree and filled by a deterministically-hashed entity-type color (no pre-existing type-color mapping was found anywhere in the app, confirmed by grep before building one), a gray/amber border (unreviewed) and an independent corner badge/dot (deferred debt) that can both render on one node at once, dashed borders/edges for batch-proposed content vs. solid for persisted context, and a compact hand-rolled force-directed layout (repulsion + spring attraction + weak centering, no D3) with positions cached in `localStorage` — a node with an existing cached position never re-runs physics at all, so reopening a graph never rearranges it and this survives a real page reload. Hover tooltip, click popover (summary, status badges, batch-mode-only Accept/Reject, "Show in list"), and multi-select (shift-click / rubber-band drag) are all caller-supplied callbacks — the module owns rendering/layout only, never selection/accept/reject state. Tasks 7.3/7.4 wired this into `review-ui/public/index.html`/`app.js`: a `[List | Graph]` toggle on Batch Review (List default) whose Graph mode shares the EXACT SAME checkboxes and `acceptSingle`/`rejectSingle` functions List already uses (the list stays in the DOM, just hidden, while Graph is showing) — multi-select flips those same checkboxes so the existing `#btn-accept-selected`/`#btn-reject-selected` bar needed zero changes to also work from Graph mode; and a new standalone "Graph" nav view (whole-graph, defaulting to the confirmed unreviewed-OR-deferred-debt filter with checkboxes, an explicit "Show everything", and client-side name/type search), with no Accept/Reject (not a review surface) and "Show in list" only when the entity belongs to a currently-open batch. `review-ui/public/graph.html` (Phase 6's deliberate stub) now redirects to the new `/#graph` SPA view. **Actually visually verified**, not assumed from source: no interactive browser was available in this environment (confirmed via tool search, same as every prior UI phase), so real headless Chromium was driven via Playwright borrowed read-only from the sibling `foundry_worldFabric` project's `node_modules` against a real running `review-ui/server.mjs` and a hand-built 11-entity/2-open-batch fixture — confirming, with screenshots AND real API responses (not just DOM state): a node (Alvor) rendering both the unreviewed amber border and the deferred-debt badge at once; accept-via-popover and shift-click-then-bulk-accept both producing `batch.mutations[...].status === 'accepted'` on disk identical to a List-mode action; layout positions byte-identical across two consecutive real page reloads; and the standalone view's default fetch genuinely returning fewer nodes (3) than `filter=all` (11) from the real network response. A self-review remediation pass (specifically re-checking, per the task file's own instruction, the three points above) found and fixed two real, related bugs only surfaced by continuing to drive the app after the initial verification: `wireRubberBandSelection`'s `window`-level mousemove/mouseup listeners were rebound on every re-render (a slow, unbounded leak across a long session), and fixing that by moving the drag-start listener onto the stable container element broke Reject/Accept from the popover entirely (the popover is also a child of that same container, so its own button's mousedown bubbled into the new listener and closed the popover before the click could fire) — caught only by watching for the actual network request and finding none, fixed with the same stopPropagation-on-mousedown convention graph nodes already used, and re-verified end to end against the real API afterward. One deliberate, documented scope limitation: an edge-only mutation has no clickable graph representation of its own (edges render as connector lines only) — accepting/rejecting it still works via List mode, matching the design doc's node-centric interaction spec rather than over-building edge-level interaction that wasn't asked for. Full test suite (root + `wf-mcp-server` + `review-ui`) passes throughout. |
| 8 | Rubber-duck creative mode | Yes | **Done** — tasks 8.1–8.5 built and tested per `plans/phase-8-tasks.md`; design followed from `plans/phase-8-review.md` as written, no redesign needed. Task 8.1: `proposeFramingsFromWriteup` (`graph-import/writeup-import.mjs`, new `prompts/writeup-framing.md`) turns a writeup into 3 cheap one-sentence interpretive framings, deliberately cheap by construction (`DEFAULT_FRAMING_MODEL = "claude-haiku-4-5"`, distinct from `DEFAULT_WRITEUP_IMPORT_MODEL`'s `claude-sonnet-5`; small `maxTokens`; retry-once-then-typed `FramingProposalError`) — a real API smoke test measured it at ~5-25s versus Phase 3's ~51-59s p50 extraction baseline and confirmed the three framings are genuinely distinct readings (as low as 9% pairwise word overlap in one run), not near-duplicate paraphrases. Task 8.2: the reject-loop's decisive design — `composeFramingNote`/`recordFramingRound`/`requestReframing`/`resolveRejectLoop`, all pure and unit-tested independent of any HTTP/MCP transport — implements the three distinct paths exactly as the design doc resolved them: an explicit note resolves straight to `{kind:'regenerate'}` (skips the framing loop entirely, reuses the existing `regenerateWriteupImport` unchanged); a quick-pick reason resolves to `{kind:'reframe'}`, informed by *why* the prior round didn't land, bounded to one round via `batch.scope.framingHistory.length` (a second plain reject throws `FramingRoundLimitError`). Task 8.3: `mutation-engine/user-settings.mjs` follows `human-review.mjs`'s exact flat-JSON/env-override/`withLock` convention for a single global `{rubberDuckMode:{enabled,updatedAt}}` toggle. Task 8.4 (`wf-mcp-server/lib/mutation-ops.mjs`, shared by both front-ends): `proposeFromWriteupOp` reads `getUserSettings()` **exactly once**, at submission time — off-mode is a byte-identical pass-through to Phase 5's `importWriteup` (confirmed: Phase 5's own `writeup-import-roundtrip.smoke.mjs` re-run completely unchanged still passes); on-mode returns phase-A framings with no batch created yet. `selectFramingForNewBatch`/`selectFramingForExistingBatch` (phase B) create or replace a batch's mutations and record the completed round onto `batch.scope.framingHistory`; `rejectWithLoopOp` is `wf_reject`'s new dispatch, a byte-identical pass-through to the old `rejectOp` for any batch that isn't a rubber-duck writeup-import batch at scope='batch'/'region'. New MCP tools `wf_select_framing`, `wf_get_rubber_duck_mode`, `wf_set_rubber_duck_mode`; `wf_reject` gained optional `note`/`quickPickReason` params. review-ui gained matching routes (`/api/settings/rubber-duck`, `/api/writeup-propose`, `/api/writeup-select-framing`) and its `/reject` route now dispatches through the same `rejectWithLoopOp`. A real, API-backed MCP-protocol smoke test (`wf-mcp-server/test/rubber-duck-writeup.smoke.mjs`) ran all 5 required scenarios in one pass against the live Anthropic API: rubber-duck OFF unchanged, rubber-duck ON full happy path, plain-reject-loops-to-new-framings, a second plain reject refused once the bounded budget is spent, and an explicit-note reject skipping straight to regenerate even past that exhausted budget. Task 8.5: a genuinely new review-ui frontend surface — a "New Import" paste-a-writeup screen (review-ui had none before Phase 8; writeup import was MCP-tool-only through Phase 6), a "First Reactions" framing-selection screen (three radio cards + blend line, reusing Phase 6's existing card/button visual language), a reject-loop quick-pick panel on Batch Review (renders only when the *batch's own* stamped `scope.rubberDuck.enabled` is true, never the live setting), and a Settings toggle. **Actually visually verified**, not assumed from source: no interactive browser was available in this environment (confirmed via tool search finding no `claude-in-chrome` MCP tools, same as Phase 6), so real headless Chromium was driven via Playwright borrowed read-only from the sibling `foundry_worldFabric` project's `node_modules` against a real running `review-ui/server.mjs`; a real, API-backed click-through drove the entire flow end-to-end (enable rubber-duck mode → submit a writeup → 3 real distinct framing cards → pick with a blend line → a real 26-mutation batch → expand the reject panel → plain-reject into a real second framing round → complete it → hit the real round-limit message on a third attempt → reject-with-note past that limit → disable rubber-duck mode and confirm the reject panel is completely absent on a normal-mode batch), 15 screenshots captured and inspected plus 2 more confirming dark-mode contrast, no bugs found. A self-review remediation pass (checking, per the task file's own instruction, specifically rubber-duck-off byte-identity, the round bound actually being enforced, and `batch.scope.rubberDuck` never being re-read live) found and fixed one real gap: `selectFramingForExistingBatch` had no bound check of its own — only `requestReframing`'s pre-flight check (gating *new* framing calls) enforced `MAX_FRAMING_ROUNDS`, so a caller invoking `wf_select_framing`/`writeup-select-framing` directly with a stale or reused `framings` array (bypassing `wf_reject` entirely) could have pushed `framingHistory` past the bound purely by committing rounds, never by requesting new ones — fixed by adding an independent, defense-in-depth bound check at the commit step too, covered by a new regression test. One deliberate, documented scope decision: the reject-loop applies only at scope='batch'/'region' (mirroring `regenerateWriteupImport`'s own established whole-batch-only design and the fact that a writeup-import batch always has exactly one region) — a scope='entity' reject, even on a rubber-duck batch, stays a normal unlooped reject, since discarding one extracted item isn't "I don't like this whole first pass." No `SCHEMA_VERSION` bump was needed (unlike Phase 5's `SourceKind` addition): `Batch.scope` was already a fully open `z.record(z.string(), z.any())`, so `rubberDuck`/`framingHistory` are purely additive untyped metadata that need no schema change — a deliberate, documented judgment call per task 8.2's own "your call" instruction. Full test suite (root + `wf-mcp-server` + `review-ui`, 20+ suites) passes throughout. |
| 10 | **Per-entity narration & persistence** — replaces whole-batch narration with per-entity narration, persisted durably (a new GM_Tools-side store, not a World Fabric schema change) and targeted using the entity's own real graph connections. Directly fixes a cluster of real bugs found in first hands-on use: identical narration across every row in a batch, "regenerate" recycling the same generic text regardless of which row it was clicked from, no memory of a previously-accepted narration across page loads, and a default "session zero"-style framing caused by narrating with zero grounding context (review-ui never passed a location/reachable-areas hint at all). See `plans/phase-10-review.md` (root cause + decisions, grounded directly in the project owner's own usage feedback rather than a persona review) and `plans/phase-10-tasks.md` (task plan). | Yes | **Done** — tasks 10.1–10.5 built and tested per `plans/phase-10-tasks.md`; design followed from `plans/phase-10-review.md` as written, no redesign. Task 10.1: new `mutation-engine/entity-narration.mjs` follows `human-review.mjs`'s exact flat-JSON/`GM_TOOLS_ENTITY_NARRATION_DIR`-env-override/`withLock` convention, but per-`(world,entityId)` file (`pending-ledger.mjs`'s precedent, not `human-review.mjs`'s per-world one — this store's query pattern is always keyed by a single entity). A genuine HISTORY, per the project owner's explicit requirement: every `saveEntityNarration` appends a new entry and marks any prior `'current'` entry `'superseded'` first — nothing is ever deleted (confirmed by direct code inspection during the self-review pass: no `filter`/`splice`/delete call exists anywhere in the module); `supersedeEntityNarration` marks current as superseded without adding a new entry, a safe no-op with no narration yet. Task 10.2 added `mutation-engine/narrate.mjs`'s `narrateEntity(batch, mutationId, ctx, opts)` — same truncation-retry handling as `narrateBatch` (factored into a shared `runNarrationCall` helper so neither duplicates it), but gated at ENTITY grain (`assertMutationNarratable` checks only the targeted mutation's own status, never looking at any sibling mutation — confirmed airtight by direct inspection, not just by test) and targeted using the entity's own real immediate (depth-1) graph neighbors (new `buildAdjacencyContext`, reusing `wf-mcp-server/lib/graph.mjs`'s `neighborhood()` — the same adjacency primitive `time-skip/resolve-pending.mjs`/`run-cycle.mjs` already import into a mutation-engine-adjacent module, confirmed real precedent before reusing it here) instead of the always-empty `currentLocation`/`reachableAreas` slots — the concrete fix for the reported generic "session zero" framing. On success, persists via `saveEntityNarration` so the result survives a reload; `narrateBatch`/`wf_narrate_batch` are unchanged and still available for a whole-scene summary. A real-API smoke test (`mutation-engine/narrate-entity.smoke.mjs`, run and PASSED against the live Anthropic API) proved the actual regression: two entities (Alvor, Gerdur) in the same accepted batch produce genuinely different, well-grounded narrations, each mentioning the other by name via their real kinship edge. Task 10.3 wired `supersedeEntityNarration` into `wf-mcp-server/lib/mutation-ops.mjs`'s `acceptMutationIds` — the single shared choke point behind every accept path in this codebase — so re-mutating a narrated entity and accepting that later batch supersedes (never deletes) its narration, proven via a real subprocess/real-MCP-protocol test. Task 10.4 added three new MCP tools (`wf_narrate_entity`, `wf_get_entity_narration`, `wf_get_entity_narration_history`, thin wrappers with no independent logic) and the matching review-ui routes (`POST .../narrate-entity`, `GET .../narration`, `GET .../narration-history`), plus a real-API route-level smoke test proving the same two-different-entities regression check at the HTTP layer with persistence confirmed via the GET routes (simulating a reload). Task 10.5 replaced review-ui's whole-batch, in-memory-only `reviewState.narrationCache` with a per-row fetch/render of that entity's own current narration (same Phase 6 serif/parchment visual treatment), a per-row "Narrate This"/"Regenerate" action wired to the new entity-grain route, and a new "View narration history" expandable affordance. **Actually visually verified**, not assumed from source: no interactive browser was available in this environment (confirmed via tool search, same as every prior UI phase), so real headless Chromium was driven via Playwright borrowed read-only from the sibling `foundry_worldFabric` project's `node_modules` against a real running `review-ui/server.mjs`, real API-backed — 11 screenshots captured and inspected proved both concrete required behaviors: narrating two different rows in one batch produced genuinely different texts (confirmed both textually and visually), and a completely fresh page load (new browser instance, no client-side state carried over) showed both previously-generated narrations already in place with zero "Narrate This" buttons remaining — the actual persistence-across-reload fix — plus regenerate-then-view-history showing both the new current and original superseded entries, full text intact, and correct dark-mode contrast with zero console errors. A self-review remediation pass (specifically re-checking, per the task file's own instruction, that the gate is genuinely entity-grain, superseded narrations are never deleted, and the frontend never shows a stale narration as current — all three confirmed clean by direct code inspection) found and fixed one real gap: `buildAdjacencyContext` had no handling for an `upsert_edge`/`delete_edge` mutation at all — the edge's own id is never a key in `neighborhood()`'s adjacency map, so it silently degraded to grounding the prompt on the raw internal edge id with zero real neighbors; fixed to ground an edge mutation on its own two real endpoints (plus each endpoint's other immediate connections) instead, covered by new unit tests and reconfirmed against the real API. Full test suite (root 21 suites, `wf-mcp-server` 5 suites, `review-ui` 37 tests) passes throughout. |
| 11 | **Per-node content generation** ("develop this node": description, secrets, potential rolls, hooks, type-specific templates per person/place/faction/object/event/concept — the generalized version of the old, deprioritized "Phase 0" NPC/location-card idea) | Yes | **Done** — tasks 11.1–11.5 built and tested per `plans/phase-11-tasks.md`, design followed from `plans/phase-11-review.md` as written, no redesign. Task 11.1's `mutation-engine/prep-content.mjs` is a genuinely separate store from the mutation-engine/review-state.mjs pipeline — **verified by directly reading `schema.mjs`'s `Mutation`/`StoredMutation` shape**, not just trusted from the design doc's own flagged caveat: `PrepContent` has no `op`/`batchId`, is never diffed against the live graph, and (per this phase's confirmed decision) is never synced to Foundry, so a separate store is the correct call, not a shortcut. Follows `entity-narration.mjs`/`human-review.mjs`'s exact flat-JSON/`withLock`/env-override convention: one file per `(world, entityId)` holding a single current doc (not a history array — the design shape has none), all six entity-type templates implemented exactly per the design doc's field lists (person/place/faction/object/event full templates, concept reduced to `description`+`howItSurfaces`). Task 11.2's `proposeFramingsForEntity`/`generatePrepContent`/`regeneratePrepField` mirror `writeup-import.mjs`'s rubber-duck framing mechanism one level down to a single entity (haiku-tier framing call, sonnet-tier generation, a bounded one-round reframe mirroring `MAX_FRAMING_ROUNDS`), both grounded in the entity's real immediate graph neighborhood by reusing `narrate.mjs`'s `buildAdjacencyContext` rather than a second adjacency walk — a real-API smoke test proved a person and a place produce genuinely different, type-appropriate field shapes (the actual regression guard against "generic mushy content forced onto everything," the specific failure mode framing-first design exists to prevent), each concretely grounded in real graph connections by name. Task 11.3 added a new shared `wf-mcp-server/lib/prep-content-ops.mjs` (same "front-ends are thin wrappers" convention as `mutation-ops.mjs`), 8 new MCP tools (`wf_propose_prep_framings`/`wf_reframe_prep_framings`/`wf_generate_prep_content`/`wf_get_prep_content`/`wf_accept_prep_content`/`wf_discard_prep_content`/`wf_regenerate_prep_field`/`wf_mark_prep_content_stale`) and matching review-ui routes under `/api/entities/:entityId/prep(...)` — deliberately no `batchId` parameter anywhere in this entire surface, confirmed both by a route-level test (greps the routes' own source for the absence of `batchId`) and by real visual verification that the resulting UI affordance never appears in Batch Review. Task 11.4 wired `markPrepContentStale` into `wf-mcp-server/lib/mutation-ops.mjs`'s `acceptMutationIds` — the SAME shared accept choke point Phase 10 task 10.3 already used for narration invalidation, reused rather than adding a second hook point, proven by a real subprocess/real-MCP-protocol test that re-mutating and accepting a later batch flips accepted prep content to `'stale'` with `fields` completely byte-identical, never deleted. Task 11.5 added a new review-ui entity-detail view (`#entity/<id>`) reachable ONLY from the standalone Graph view's node popover (Phase 7) via a new "Develop this node →" link gated by `mode === 'standalone'` in `graph-view.js`'s own rendering code — structurally, not just conventionally, absent from Batch Review's List/Graph toggle, which never passes the callback that renders it. The page implements the full pipeline (develop button → framing pick, reusing Phase 8's exact `.framing-cards` visual pattern including its "none of these, write your own" option → type-specific field cards, plain text or a bulleted skill/DC/purpose list for `potentialRolls` → Accept/Discard while `'proposed'` → an independent per-field Regenerate control once accepted) plus a quiet, amber-left-border staleness badge (matching this project's established "mild interrupt" convention, not an alarming banner) that never hides the content underneath. Actually visually verified via real headless Chromium (no interactive browser was available in this environment, same as every prior UI phase) against a hand-built fixture graph, real API-backed: 22 screenshots confirmed a person and a place produce visibly different field sections grounded in real graph connections named by name, regenerating one field changes only that field (confirmed by a full before/after text diff of every field), a staleness marker appears with all fields still fully rendered after a real Batch-Review-driven re-mutation and accept, "Develop this node" is absent from Batch Review in both List and Graph mode (a DOM query scoped to the Review view, zero matches), discarding a draft persists correctly across a real page reload, and dark-mode contrast/zero console errors held throughout. A self-review remediation pass re-confirmed, by direct grep, that no `wf_sync_to_foundry`/`wf_apply_mutations`/`applyMutationsToFoundry`/`applyHeadless` code path references prep content anywhere, that no route or MCP tool anywhere accepts more than one `entityId` at a time (no bulk "develop all" affordance exists), and that `markPrepContentStale` only ever flips `status` — direct inspection confirms no delete/unlink call anywhere near it. Full root/wf-mcp-server/review-ui test suites all still pass. See `plans/phase-11-review.md` and `plans/phase-11-tasks.md`. |
| 12 | **Interactive graph editor** — turns Phase 7's read-mostly graph view into a real editing surface: manual node/edge create/edit/delete (immediate-write, no review gate), an LLM-assisted "scan for mentioned entities" workflow (goes through the existing accept/reject flow like everything else AI-proposed), narration reset, four new World Fabric schema fields (status/lifecycle, GM-only visibility, canon-lock, PC/NPC distinction — all confirmed by the project owner), and a dedicated single-slot "Undo Last Manual Edit" mechanism (flagged by the interaction-design review as the single most important piece, since manual edits bypass the batch/rollback system entirely). Surfaced by real hands-on feedback: a concrete workflow gap (generated content mentioning "an arena champion" or "a quartermaster" with no way to turn those into real linked entities) and a confirmed missing capability (no way to delete an entity at all — discovered via real stray data, `alvor`/`riverwood`/`gerdur`/`sven`, sitting in the project owner's actual `wf-test` world with no way to remove it). See `plans/phase-12-review.md` and `plans/phase-12-tasks.md`. | Yes | **Done** — tasks 12.1–12.6 built and tested per `plans/phase-12-tasks.md`; design followed from `plans/phase-12-review.md` as written, no redesign. **Task 12.1** is this project's first genuine additive change to the separate `foundry_worldFabric` repo (not just reading from it): four new plain scalar entity fields (`status`/`playerKnown`/`canonLocked`/`role` — deliberately NOT `attributeDefs`, which are reference-kind fields that auto-derive an edge; see `constants.mjs`'s own doc comment) wired into both `graph-service.mjs`'s `upsertEntity`/`applyMutations` (live path) and `interchange.mjs`'s `normalizeEntity` (headless path, shared by every GM_Tools write). Per-type status defaults (`STATUS_OPTIONS_BY_TYPE`) leave `event`/`concept` at `status:null` (no lifecycle concept) rather than forcing a fake enum. A new, deliberately-factored-out-and-exported `migrate-entity-fields.mjs` backfills these four fields onto entities persisted before this change — required because the additive field-list change alone never reaches an already-persisted entity record, the same class of gap this project hit twice before for entityTypes' attributeDefs (Phase 1.5b/1.5c) — wired into `module.mjs`'s `_runMigration`. **Live-reverified against the real `wf-test` world's exported snapshot** (not a synthetic fixture): confirmed all 18 real entities, including `alvor`/`riverwood`/`gerdur`/`sven`, predated the four fields and backfill cleanly and idempotently to sensible per-type defaults (`test/migrate-entity-fields.test.mjs`, run directly against `/home/russell/foundrydata/Data/worlds/wf-test/world-fabric-snapshot.json`). GM_Tools' `writeup-import.mjs` RawWfiEntity schema and `diff.mjs`'s field list extended additively to match. **Task 12.2** added the four new fields plus importance/session-staleness (a best-effort constant matching World Fabric's own default, not a live per-world read — metadata nomination #5 was explicitly confirmed GM_Tools-side-only/no-schema-change)/foundryRef-presence/edge strength-valence-notes to `/api/graph`'s node/edge payload — pure additive wiring, no new backend logic. **Tasks 12.3/12.4** (built together per the task file's explicit instruction) added `wf-mcp-server/lib/manual-edit-ops.mjs`: manual node/edge create/edit/delete as immediate writes reusing the exact same live-then-headless `applyMutationsWithHeadlessFallback` path every other write in this project uses, with every create pre-assigning its own id (stamped onto both the mutation's top-level `id` and `data.id`) so the live path's well-known "can't report a created id back" gap never applies to a manual write at all. New `mutation-engine/manual-undo.mjs` is the single-slot "Undo Last Manual Edit" buffer, deliberately mirroring `rollback.mjs`'s own simplicity (last-write-wins, no history list) — covers all six manual-write kinds plus narration reset; a node delete captures its own pre-state AND every cascaded edge's pre-state as ONE undo action, restored together in a single apply call. Manually-created/edited entities are confirmed to never render unreviewed-amber (every write calls `markHumanReviewed`, and a genuinely new entity has no unreviewed-tracking entry at all by construction). `review-ui/public/graph-view.js` gained the whole standalone-mode-only editing surface (gated behind a new `opts.editable` flag, structurally absent from Batch Review, same guard convention as Phase 11's `onDevelopNode`): a shared `svgPointFromClient()` coordinate helper (reused by both new click-coordinate consumers, per this project's own standing warning never to invent a second coordinate-math approach), press-and-hold edge drawing with a 5px threshold and re-drag-onto-existing-same-type-pair reuse, click-to-open edge popovers, inline create/edit forms, and delete confirms showing the real cascade count. **Task 12.5** added `graph-import/scan-mentions.mjs`: scans a block of text (primarily Phase 11 prep content) for entity mentions, reusing `writeup-import.mjs`'s `previewWriteupImport` (and therefore `importGraph`'s real name+type dedup) rather than a second matcher — LINK and PROPOSE-NEW are genuinely different mutation shapes (a link is a single edge-only mutation with no entity write at all; propose-new is an entity create plus an edge, two mutations), not cosmetic labels on one path, confirmed by a dedicated deterministic unit test and routes through the existing review-state batch/accept/reject flow unchanged, per decision 3. A new `patchPendingMutationData` (`mutation-ops.mjs`) backs the design doc's editable-relationship-type-dropdown requirement for a pending link row. **Task 12.6** extended `entity-narration.mjs` with an additive optional `origin` field (SCHEMA_VERSION 1→2) so a reset's deliberately-empty entry is distinguishable from a real narration; `resetEntityNarrationOp` appends a new empty `'current'` version (never deletes) and records the prior prose onto the manual-undo slot so undo restores it as a further append, never a delete either. `schema.mjs`'s `SCHEMA_VERSION` bumped 3→4 for the new `'mention-scan'` `SourceKind`. Deliberately **not exposed as MCP tools** at all: manual edits bypass the review gate by design (decision 1), and an LLM-driven MCP call reaching that same unreviewed write path would undermine the no-silent-auto-write invariant this whole project is built around — confirmed by direct grep that no `wf_*` tool references any manual-edit-ops function. **Actually visually verified** via real headless Chromium (no interactive browser available in this environment, confirmed via tool search, same as every prior UI phase) against a hand-built fixture graph, real API-backed for the scan-for-mentioned-entities LLM call: placing a node at the exact clicked point, drawing an edge and having the type/label/strength/notes prompt open immediately with a real screenshot of the prompt, editing an existing node's fields directly, deleting a node with the real cascade count shown ("...and its 2 connected edges?") and confirming both cascaded edges come back atomically via Undo, the two genuinely-distinguishable scan-mention badges (🔗 green "Existing" vs ➕ amber "New", both border-accented) with correctly-counted split accept-all buttons, and a narration reset followed by confirming the original prose is still readable in "View narration history" — zero console errors across the full driven flow, dark-mode contrast confirmed separately. Three real bugs were found and fixed ONLY by actually driving this in a browser, not by reading the code or by the unit/route test suites (which could not have caught any of them, since all three are DOM-event-ordering bugs with no server-side symptom): `armPlacementMode`'s click handler didn't stop propagation, so a placement click landing on an existing node also triggered that node's own popover afterward, removing the just-opened create-node form before it could be used; a popover button whose own click handler removes/replaces part of the popover's DOM (`showNodeDeleteConfirm`) detached `evt.target` from the popover mid-bubble, so the popover's outside-click dismiss listener (`el.contains(evt.target)`) wrongly saw "outside" and closed the whole popover out from under the confirm UI that same click had just opened — fixed by switching both outside-click checks to `evt.composedPath().includes(el)`, captured at dispatch time and immune to later DOM mutation; and a plain `ReferenceError` (`onKeyDown` referenced before it was ever an in-scope binding, a named-function-expression scoping mistake). A related gap found during the same visual pass and fixed: the entity-detail narration section's early `return` for "no current narration" (including immediately after a reset) skipped the "View narration history" toggle entirely — exactly the case where proving history survived matters most. Full test suite (root 24 suites, `wf-mcp-server` 7 suites including a 19-case direct-call undo/cascade test, `review-ui` 67 tests) passes throughout; `foundry_worldFabric`'s pre-existing unrelated uncommitted changes (`package.json`, `scripts/apps/cockpit-app.mjs` modified; `setup-test.mjs`, `test/e2e-m13a.mjs` untracked) confirmed untouched. One deliberate, documented scope decision: the design doc's secondary narration-reset "quick-path" from the graph popover itself was not built in this pass (the primary entity-detail-view path is fully functional and was what got visually verified) — a known, small scope trim under this phase's effort budget, not an oversight. |
| 13 | **Graph editor remediation (round 2 hands-on feedback)** — four fixes from a second real session with Phase 12's editing surface: defer manual-edit Foundry sync (headless-immediate write + a batched, explicit sync action later, mirroring the existing batch accept→sync pattern, instead of every micro-edit independently paying the ~7s live-Foundry-poll cost); the graph layout scaling with node count instead of packing everything into a fixed 960×620 area, plus real click-and-drag panning for the standalone view; a correction path letting a "propose new" mention-scan result be redirected to link an existing entity instead of creating a duplicate; and a lightweight deterministic pre-pass to catch more existing-entity matches before they're ever proposed as new. See `plans/phase-13-tasks.md`. | Yes | **Done** — tasks 13.1–13.4 built and tested per `plans/phase-13-tasks.md`, no new design round (the project owner's own hands-on feedback was concrete enough to build from directly, matching prior remediation rounds' discipline). **Task 13.1** is the most architecturally significant: `wf-mcp-server/lib/manual-edit-ops.mjs`'s six write functions (add/edit/delete node, add/edit/delete edge) plus undo's own inverse no longer call the old dual-path `applyMutationsWithHeadlessFallback` at all — a new `applyManualMutations` appends each write to the world's ONE ongoing "manual-edit" `review-state.mjs` batch (created lazily, reused across edits until synced), marks it accepted immediately via `rollback.mjs`'s own `acceptMutations` (real pre-state capture, rollback-batch eligibility as a side benefit), and applies HEADLESSLY RIGHT AWAY via `applyHeadless` directly — no live-Foundry-poll cost on the interactive path at all. Because the batch's mutations are `status:'accepted'` while the batch itself stays `status:'open'`, the EXISTING, completely unmodified Sync button/route (`syncOp`) already knows how to push it through the real live-first dual-path apply when triggered — proven for real, not assumed, by `wf-mcp-server/test/manual-edit-sync.test.mjs`, which shows a later sync genuinely writes to and polls the real `world-fabric-mutations.json` bridge file (a background loop simulating a live Foundry client, exactly like `sync-headless.test.mjs`'s own established pattern) before falling back headless. Undo's own inverse mutations were deliberately routed through the SAME headless-immediate mechanism (not the old dual-path function) — reasoned through explicitly in the code: routing undo through the live bridge independently of the batch it's undoing could let a live client apply the inverse while the original still-unsynced mutation sits unremoved in the batch, so a later sync would resurrect exactly what was just undone; keeping both on the same batch-accumulation path means a create-then-undo nets out to a syncable no-op instead of a correctness gap. All six Phase 12 undo action types (plus narration reset) re-verified passing after the write-path change. review-ui gained a standalone Graph view sync bar ("N manual edits not yet synced to Foundry") mirroring the Review screen's existing pattern exactly, backed by a new read-only `getManualEditSyncStatusOp`/`GET /api/manual-edit-sync-status`. **Task 13.2**: `review-ui/public/graph-view.js`'s previously-fixed 960×620 layout box now grows via `layoutDimsForNodeCount` (scaling both dimensions together with `sqrt(nodeCount/8)`, keeping ideal per-node spacing roughly constant as a graph grows instead of shrinking toward zero) with a safeguard (`layoutDimsForCurrentGraph`) ensuring the box never shrinks below already-cached node positions' own bounding box. Every function that used to read the fixed `LAYOUT_W`/`LAYOUT_H` module constants now reads the rendered SVG's own live `viewBox` (`currentLayoutDims`) instead — one fewer place for a second copy of dims to go stale across a re-render, and the same zoom-level-independence property the file's ratio-based coordinate math already relied on. Zoom now scales both CSS width AND height directly off the current dims (previously width tracked the container at 100%, which would have silently squeezed a bigger viewBox back down and defeated the entire point of more room) at a fixed px/logical-unit matching the historical baseline-graph size exactly, so a small graph's zoom-100% appearance is unchanged byte-for-byte. Real click-and-drag panning (`container.scrollLeft`/`scrollTop`, not a viewBox/transform change) was added for the standalone Graph view ONLY, mutually exclusive by construction with batch mode's own rubber-band background-drag. **Real visual verification**: a genuine 18-entity/20-edge fixture confirmed the reported overlap problem BEFORE this change (min pairwise node distance ~44 layout units, several nodes visibly touching/overlapping in a real screenshot) and confirmed the fix AFTER (min pairwise distance 60–100+ units, visibly separated in a real screenshot at the same zoom level); further screenshots confirmed popover anchoring stays correct, zoom-in to 175% renders correctly, and a real drag gesture genuinely moves `container.scrollLeft/scrollTop` and reveals nodes that started off-screen. Batch-mode rubber-band selection and popover-driven Accept were separately re-verified against the new per-render dims (a 3-mutation batch fixture): rubber-band correctly selected exactly the 3 pending nodes with the List view's own checkboxes reflecting the identical state, and a popover Accept click completed with zero console errors. **Task 13.3**: new `redirectMentionScanRowToExistingOp` (`wf-mcp-server/lib/mutation-ops.mjs`) converts a still-pending "propose new" mention-scan row (op:`upsert_entity`, `entityContext.scanResultKind==='new'`) in place (same `mutationId`) into a genuine `upsert_edge` targeting a GM-chosen existing entity, removing the sibling edge-to-would-be-new-entity mutation entirely — leaving exactly ONE real link mutation, not a modified create, gated to "propose new" rows only (never a LINK row, already correct by definition, and never a non-mention-scan batch). A self-review pass added a defense-in-depth validation (`existingEntityId` must resolve to a real live entity, matching `addEdgeOp`'s own established convention) and fixed the resulting row's `entityContext` to reflect the TARGET entity's own real name/type rather than the stale original mention's guessed type. review-ui gained a "Link to existing instead" button (propose-new rows only) opening an inline searchable entity picker reusing the standalone Graph view's own `GET /api/graph?filter=all` + client-side name/type filter, per the task's own explicit instruction not to build a third search UI. Verified end to end, not just UI-deep: a unit test proves accepting the batch afterward creates the edge (not a duplicate entity), a route-level test covers the HTTP boundary, and real headless Chromium confirmed the row's badge/border genuinely flips from "➕ New" to "🔗 Existing" and the batch's own "Accept All Links"/"Accept All New Entities" split-bar counts re-derive correctly after redirect. **Task 13.4**: a new, purely deterministic (no LLM call) pre-pass in `graph-import/scan-mentions.mjs` runs alongside (never replacing) the exact case-insensitive name+type dedup (`interchange.mjs`'s `findExisting`, which by construction can never catch a near-miss) — combining stopword-stripped token-Jaccard overlap (catches a dropped/added filler word, e.g. "Gorrim Smith" vs "Gorrim the Smith") and single-token edit-distance ratio (catches a minor spelling variant on an otherwise one-word name) into one conservative `nameSimilarity` score (`FUZZY_MATCH_THRESHOLD = 0.75`, confirmed against real test cases that "Kael" vs "Kaelen" — similarity ~0.67 — correctly stays below it while both real near-miss shapes score comfortably above). The mechanism is deliberately non-invasive: `applyFuzzyPrepass` REWRITES a near-miss mention's name to the matched existing entity's own exact stored name BEFORE the real exact matcher runs, so a fuzzy match resolves through the SAME classify-and-shape code path a genuine exact match would — "prefer it (a link) over a blind create" achieved by influencing the input, not duplicating logic. 17 unit tests cover both required cases directly and end-to-end through `previewMentionScan`: a deliberately-constructed near-miss the exact dedup would miss becomes a genuine LINK, and two genuinely different, similarly-named entities are confirmed NOT incorrectly merged. The existing real-API `scan-mentions.smoke.mjs` was re-run to confirm the pre-pass doesn't interfere with the live extraction path (still correctly produced one exact LINK and two genuine PROPOSE-NEW results against real generated-content-shaped text). One noted, deliberate limitation from the self-review pass (not a regression, an inherent tension with an existing, valued invariant): task 13.2's overlap fix takes full effect for any graph laid out fresh or as new nodes are added over time, but a real *already-cached* graph's existing node positions won't retroactively reflow, since `computeLayout`'s own "never rearranges, even across a reload" guarantee (an explicit Phase 7 requirement) is deliberately preserved rather than silently broken to force a relayout. Full root (24 suites) / `wf-mcp-server` (9 suites) / `review-ui` (72 tests) suites pass throughout. A persona-driven QA pass follows as a separate, explicitly-deferred next step per the project owner's own request, not part of this phase's own execution. |
| 14 | **QA pass remediation** — nine real, independently-confirmed bugs/gaps found by two DM-persona QA passes actually driving the tool in a browser (not suspicions — each verified against real data/code/measured behavior). Two critical: rollback can silently report false success when a batch has two mutations touching the same field (root-caused: restores applied in original chronological order let a later, less-historical preState overwrite an earlier one via headless-apply's shallow merge — fix is reversing the restore order); and a brand-new world is a dead end (`bootstrapSnapshot()` exists and is tested but never called from any production path, no "create world" UI affordance anywhere). Plus: deferred-debt neighbor entries can silently vanish via a shared-regionId bug in resolve-pending's fan-out; developing/accepting a node's content doesn't clear its unreviewed flag (`prep-content-ops.mjs` never calls human-review tracking, unlike every other write path); a node-edit popover can render off-screen near the bottom of the canvas; the Long-Unreviewed list shows raw ids instead of names (found independently by both personas); "Narrate This" isn't reachable from the standalone entity page; and scan-for-mentioned-entities has no request cancellation, so a naive retry creates a duplicate batch. See `plans/phase-14-tasks.md`. | Yes | **Done** — all eight tasks (14.1–14.8) built and tested per `plans/phase-14-tasks.md`, in severity order, no new design round (each QA finding already stated a confirmed root cause and fix direction). **Task 14.1**, the most important correctness fix in this phase: `rollback.mjs`'s `rollbackBatch` built `restoreMutations` by iterating `batch.mutations` in ORIGINAL chronological order — since every restore mutation targeting the same entity/edge id applies in array order with the LAST one winning (headless-apply.mjs's `mergedWfiRecord` merges each restore onto whatever state exists at APPLY time, not accept time), a batch with two accepted mutations touching the same field left the LATER (less-historical) preState winning while still marking every mutation `'rolled-back'` — a false-success report. Fixed by reversing the iteration order (the earliest mutation's preState now applies last and wins) — a small, surgical change, not a rearchitecture. A new regression test drives the REAL `applyHeadless`/`bootstrapSnapshot` path (not a simplified stand-in) to prove rollback now restores state from before the FIRST mutation, not just the last; confirmed the test fails against the pre-fix iteration order and passes with the fix; all existing single-mutation-per-entity rollback tests (the common case) pass completely unchanged. **Task 14.2**: `bootstrapSnapshot()` (already built, already tested) was never called from any production code path — a genuinely new campaign had no UI affordance to create a world at all. Added `POST /api/worlds` (validates the id is path-safe, rejects an already-taken id with a clean 409) and a minimal "Create New World" card in Settings (id field + button, auto-selects the new world on success). A route test starts from GENUINELY NOTHING on disk (no `bootstrapSnapshot` call in its own setup) and proves creation + immediate visibility in `GET /api/worlds`; real headless-Chromium verification drove the full UI cycle end to end from nothing on disk — create world → New Import (real API call) → Review → Accept Selected → Sync to Foundry (headless path) → confirmed entities genuinely persisted to the new world's own snapshot file. **Task 14.3**: `resolvePending`'s fan-out recorded a `resolvedPendingEntries` entry for EVERY entity folded in as context, regardless of whether `textureRegion`'s call actually produced a mutation for it; since every mutation in a resolve-pending batch shares one `regionId`, `applyLedgerOutcome` resolved ALL of them on any single accepted mutation in that region, silently clearing a genuinely-untouched neighbor's real backlog. Fixed by only recording (and only locking into `'proposed'`) entries for entities the batch's mutations genuinely target (via `m.id` for entity ops, or the edge's real endpoints for edge ops) — an untouched neighbor now stays fully `'pending'`, available for a future resolve, immune to the rest of the batch's outcome. A regression test reproduces the exact leak (a neighbor with zero mutations) and confirms its entries survive accepting the rest of the batch unchanged; a companion test confirms genuinely-mutated entities are still correctly resolved; the existing fan-out-cap test's mock was corrected (it had been asserting the bug's own assumption) without changing its cap-selection assertions. Noted, not fixed (out of this task's file scope, flagged for a future pass): `time-skip/run-cycle.mjs`'s growth-bound sweep builds `resolvedPendingEntries` the same unfiltered way for its own "bloated" entity set — the same root cause, a different call site. **Task 14.4**: `prep-content-ops.mjs`'s `acceptPrepContentOp` never called `markHumanReviewed`, unlike every other write path in the project — fixed with a one-line addition; a real MCP-subprocess test proves the underlying human-review record now exists (not just "absent from the flagged list," which would be a false-positive-prone check for an entity with no history at all). **Task 14.5**: `positionPopoverAt` anchored a popover's top-left corner exactly at the click point with no awareness of its own rendered size or the container's currently-visible viewport; reproduced via real bounding-box measurement (a 40-node fixture, scrolled so a bottom node sits near the visible edge) showing an edit form's right/bottom edge extending past the container's bounds with no way to discover it (mouse-wheel is captured for zoom, not scroll). Fixed with a new `clampPopoverIntoView`, called after each of the five real popover call sites' own DOM append, shifting the already-computed position by exactly the measured overflow against the container's real visible rect. Before/after screenshots and a real Playwright click on Save (succeeding post-fix) confirm both the horizontal and vertical cases. **Task 14.6**: `GET /api/unreviewed-entities` now looks up each flagged entity's real name from the live snapshot (mirroring `pendingEntitiesPayload`'s exact convention) instead of the raw id both QA personas independently flagged; a route test and a real screenshot confirm the Queue now shows names. **Task 14.7**: `narrateEntity()` is fundamentally scoped to an accepted mutation within a batch and the standalone entity page has no batch context — took approach (a) from the task's own reasoning (reuse existing infrastructure): a new `findMostRecentAcceptedMutationForEntity` scans batches newest-first for the most recent accepted entity-targeting mutation and narrates that via the same `narrateEntity()` call the batch-scoped path uses; a typed `NoNarratableBatchError` (mapped to a clean 404, never a silent no-op) covers the "nothing to narrate yet" case. Deterministic route tests cover the gate; a real-API smoke test proves the success path (correct batch found, real prose, durably persisted); real headless-Chromium verification confirmed both cases — a narratable entity produces real narration, an untouched entity shows a clear, specific message. **Task 14.8**: navigating away mid-scan didn't cancel the request server-side (it silently completed and created a batch the user never saw), and a natural retry created a genuine duplicate. Fixed both halves: client-side, a shared `AbortController` cancelled on every navigation; server-side, a lightweight IN-MEMORY (not a new persisted store) `dedupedScan` keyed on (world, entity, text) reuses an in-flight-or-very-recently-settled result rather than starting a new one (a failed scan is never cached, so real retries after errors still retry). Five deterministic unit tests prove the dedup mechanism with no API call; a real-API test fires two genuinely simultaneous requests and confirms exactly one batch; real headless-Chromium verification reproduced the exact reported flow (click Scan, navigate away immediately, retry immediately) and confirmed exactly one batch existed throughout. **Self-review remediation pass**: re-confirmed both explicitly-flagged regression risks (14.1 against the original single-mutation rollback tests, 14.3 against a normal no-fan-out single-entity resolve-pending call) still pass; found and fixed one real gap — `dedupedScan`'s in-memory cache only evicted a FAILED scan, so a successfully-settled entry lingered for the server process's entire lifetime (unbounded growth over a long session) — bounded to roughly one dedupe-window's worth of history via a scheduled, guarded removal. No new persisted data store was introduced this phase (`dedupedScan`'s cache is pure in-memory), so no `.gitignore` gap. Full root (24 suites) / `wf-mcp-server` (10 suites) / `review-ui` (88 tests) suites pass throughout; four real-API smoke/live tests (task 14.2's full New Import cycle, task 14.7's narrate-entity-standalone success path, task 14.8's real double-trigger, plus the pre-existing `routes-live.smoke.mjs` suite) all independently confirmed passing with a real Anthropic API key. |
| 15 | **Frontend regression suite + mobile responsive fixes** — the two concrete, non-speculative recommendations from a real frontend-architecture tradeoff study that concluded "don't change the tech stack." (1) A committed, replayable Playwright regression suite for four real interaction bugs already found and fixed once across this project's history (a popover dismiss-race, a mousedown-into-rubber-band-drag swallow, a placement-click landing on an existing node, a re-render listener leak) — every prior "verified in a real browser" claim across 14 phases has been a one-off manual/agent-driven session with nothing preventing a silent regression. (2) A real mobile-viewport pass (iPhone 13 emulation) found the world-select dropdown and Settings gear pushed completely off-screen on every view, the Batch Review action bar overflowing horizontally and clipping "Reject Selected," and 13×13px checkboxes well under the ~44px touch-target guideline — this codebase had never once been designed for or tested against a touch/mobile viewport. See `plans/phase-15-tasks.md`. | Yes | **Done** — tasks 15.1–15.5 built and tested per `plans/phase-15-tasks.md`, no new design round needed (the task file's own confirmed root causes were accurate and complete). **Task 15.1**: `review-ui/package.json` (previously zero dependencies of any kind, by its own stated convention) gained its first real devDependency — Playwright (`^1.62.0`), documented in the package description as dev/test-only, never imported by `server.mjs` or any runtime path; a new `npm run test:e2e` script (`node --test test/e2e/*.e2e.mjs`) stays deliberately separate from the fast deterministic `npm test`. Confirmed via `npm ls --omit=dev` that it never appears in a production-only listing. **Task 15.2**, the most important task in this phase: four real Playwright tests (`review-ui/test/e2e/*.e2e.mjs`) drive an actual running `server.mjs` instance (in-process `createReviewServer({port:0})`, the same scratch-dir isolation convention as `test/routes.test.mjs`) against real fixture data, each reproducing the EXACT original failure mode documented in `graph-view.js`'s own "found via ... in a browser" comments rather than a generic approximation — the popover dismiss-race (`composedPath()` vs. the old `el.contains(evt.target)`, reproduced by clicking a node's delete icon and confirming the confirm UI survives and a real delete completes), the mousedown-bubbling swallow (Accept from a batch-mode graph popover, confirmed via the real on-disk mutation status transitioning to accepted), the placement-click-lands-on-existing-node bug (confirmed via the create-form's own title, not the existing node's popover, opening), and the rubber-band window-listener leak (instrumented via a direct `window.addEventListener` call-count — the task file's own suggested "more direct way" — proving the count stays at exactly one pair across several real re-renders triggered by genuine accepts, not just an indirect "does drag-select still work" proxy, which is also kept as a secondary check). The suite's own validity was proven twice, not just asserted: the `composedPath()` fix and the mousedown-`stopPropagation()` fix were each independently, temporarily reverted, their corresponding test confirmed to genuinely fail against the reverted code, then re-applied with `git diff` confirming a byte-identical restore. **Tasks 15.3–15.5** (mobile responsive fixes) were combined into one commit — the three touch the same interconnected topbar/action-bar/checkbox markup and CSS and were reproduced/fixed/verified together in one real iPhone-13-emulation session (390px logical viewport), each with real bounding-box measurements and screenshots before and after, matching how the bugs were originally found. 15.3: the world-select dropdown and Settings gear (confirmed off-screen: `x=384`/`x=542` against a 390px viewport) are now reachable via a new hamburger-toggled drawer (`#topbar-collapsible`, `display:contents` at desktop so desktop layout is byte-identical, `position:absolute` overlay at mobile so `--topbar-h`'s sticky-positioning math for `.action-bar`/`.narrate-bar` is completely untouched) — closes on nav-click, a world change, AND the `hashchange` listener itself (a real gap caught mid-verification: a hash-only "navigation" that doesn't change the hash string never fires `hashchange`, so both the click handler and the listener close it, covering every real path including browser back/forward). 15.4: the action bar (confirmed overflowing: `scrollWidth` 395 vs. `clientWidth` 371, clipping "Reject Selected" with no scroll affordance) now wraps (`flex-wrap:wrap`, a no-op at desktop, reconfirmed via a real 1280px screenshot) with Accept/Reject prioritized via `order` onto the first wrapped row ahead of the bulk-select helpers. 15.5: the mutation-row checkbox (confirmed at the browser-default ~13×13px) is now wrapped in a padded `<label class="row-check-wrap">` (native label-click-toggles-control semantics) grown to 44×44px only inside the mobile breakpoint, leaving desktop density unchanged (reconfirmed: desktop wrap box stays ~20×19px); `.icon-btn` (Settings gear, the new hamburger, graph popover edit/delete icons) got the same scoped fix, found while in the same code rather than left as a second undersized instance. Real desktop-viewport screenshots (Queue, Batch Review list, Batch Review graph, standalone Graph, Settings, all at 1280px) confirmed byte-identical layout to pre-Phase-15. Full root (24 suites) / `wf-mcp-server` (11 suites) / `review-ui` (90 deterministic tests) suites and the new 4-test `npm run test:e2e` suite all pass. A self-review remediation pass re-ran the revert-and-confirm-failure validation on a SECOND test (the mousedown-bubbling fix, independent of the one validated during 15.2 itself) as an added sanity check, and re-confirmed desktop non-regression via the screenshot pass described above. |
| 16 | **Session Planner — engine + API** — a deterministic (non-LLM) path-corridor graph traversal (generalizes the existing `neighborhood()` BFS primitive to a multi-stop route with a tolerance band, degenerating to plain radius-from-point for a single home base), a lightweight session-scoped "scenes" store (forkable, not a World Fabric entity — going off-plan is a cheap first-class action, not a special case), an always-visible ambient neighborhood digest (hard-capped, renders empty rather than padded for undeveloped nodes), two independent flags (content-readiness vs. structural under-connection — deliberately not merged), and notes-as-deferred-intake (zero-friction entity-anchored capture, batched through the *existing* writeup-import/scan-mentions pipeline rather than a new store, landing on the existing review/accept gate — never auto-writes to canon). Design grounded in two initial expert reviews, a web-research pass on prior art (Realm Works, Kanka, World Anvil, TiddlyWiki transclusion, Roam linked-references), and four DM-archetype persona reviews run twice (minimal-prep/Sly-Flourish, improv-heavy/no-prep, node-based/Alexandrian, West-Marches/sandbox) — see `plans/phase-16-review.md` for the full record, including a corrected-in-place design mistake (an initially-proposed "goal-adjacency via a new `resolution` edge type" mechanism was replaced with the much simpler path-corridor-distance approach once the motivating example turned out to be spatial, not semantic — the schema-change idea is preserved as a deliberately-deferred item, not built). UI (the actual "Plan a Session" page, hyperlinked brief rendering, annotation popover) is explicitly out of scope for this phase — see `plans/phase-16-tasks.md`. | Yes | **Design done, task-planned, not yet built** — `plans/phase-16-tasks.md` specifies a QE-first process (task 16.0 writes a full contract-defining test suite against not-yet-existing modules, confirmed red, before any implementation task starts). |
| 17 | **Session Planner UI** — the frontend for Phase 16's engine (no new server routes needed, Phase 16 already shipped everything this consumes). A "Plan Session" view rendering the brief as anchor-location-plus-satellite-cards with deliberately non-monotonic render order (never sorted by distance — that's chapter numbering with the digits filed off, per two independent findings across Phase 16's and this phase's review rounds), an always-visible ambient digest (conspicuous empty state, never blank), two independently-badged flags, inline-expand note capture (not a popover — a real bug was caught and avoided pre-build: reusing the existing popover's dismiss helper unmodified would have silently discarded an unsaved note on outside-click) with correct debounce/blur/hashchange-flush semantics, a split (not summed) beyond-corridor summary, and a race-guarded type-ahead re-center control. Grounded in a UX review and a webdev performance/testability review, both run against the actual shipped Phase 16 code. See `plans/phase-17-review.md` (design record) and `plans/phase-17-tasks.md` (task plan, same QE-first process as Phase 16). | Yes | **Design done, task-planned, not yet built.** |
| 18 | **Encounter guidance** — bring-your-own-monster-list and bring-your-own-party-sheet ingestion (pasted text or PDF, sent natively to Claude's document support), an LLM extraction step (raw fields only — all scoring is deterministic pure functions, never LLM judgment). The core mechanism is deliberately NOT a round-by-round simulator (that design was reviewed, then explicitly deferred — preserved in `plans/phase-18-review.md` §7 for possible later revisit via the separate RL-training track) — v1 is a pure extraction-plus-scoring heuristic: action-economy math plus a six-axis effect-impact taxonomy (researched across D&D 5e, Draw Steel, Daggerheart, and WoW/LoL crowd-control design), a dominant-axis-plus-decay aggregation function (not flat summing, to avoid overweighting multi-axis "save or suck" effects), a burst-ceiling number and a two-candidate snowball-delta number as non-simulated risk signals, a pack-coefficient knob, and an opt-in `getPartyContext()` grounding API for other LLM tools. Reviewed twice: round 1 (combat systems designer, simulation/quant designer, two DM personas) shaped the now-deferred simulator; round 2 (a game-balance designer, after taxonomy research) shaped the scoring heuristic that replaced it. See `plans/phase-18-review.md` (design) and `plans/phase-18-tasks.md` (task plan, same QE-first process as Phase 16/17). | Yes | **Done, built and tested.** Addendum (found during Phase 19's QE pass, fixed before Phase 19's UI was built): the shipped `encounter-suggest` route unconditionally called the real thematic-filter LLM on every request with no way to filter by attendance or score a manual combination — directly contradicting Phase 19's design. Fixed additively (`themeText`/`attendingMemberIds`/`manualCombination`, all optional, all backward-compatible; new `encounter-heuristic.mjs` export `scoreCombination` shares the exact scoring core `suggestEncounter` already used) rather than worked around in the frontend. |
| 19 | **Encounter Builder UI** — the frontend for Phase 18's engine (no new server routes needed). A difficulty-rail entry point that doubles as both "declare intent first" and "one-click gut-check" depending on how far a DM goes with it; a roster/attendance strip that's a pure client-side filter over the persisted party pool (serves a stable table and a West-Marches-style churning table with the same mechanism); a score band with permanent, mode-independent snowball-delta and burst-ceiling fixtures (named across personas as the actual reason anyone opens the tool) plus a Plain/Precise toggle defaulting to Plain; two deliberately separate disclosures (editable knobs vs. a read-only "why this score" explainer); a catalog with per-system facet reveal (not one forced shared schema), a six-axis "impact fingerprint" as a genuinely cross-system filter, and score confidence encoded in the number's own shape (solid decimal / dashed banded range / "unscored" pill, one consistent layout slot); and only two LLM call sites in the whole screen (ingestion, an optional theme-narrowing box) — everything else deterministic and instant. Grounded in a real multi-round focus group: research on existing tools (Kobold Fight Club, D&D Beyond, Foundry's PF2e/Draw Steel encounter modules), four DM personas reacting twice (open-ended, then against a concrete proposal), two UX designers grounding their proposals directly in the shipped Phase 18 code. No organic signal for maps/positioning emerged across any round. See `plans/phase-19-review.md` (design) and `plans/phase-19-tasks.md` (task plan, same QE-first process as Phase 16/17/18). | Yes | **Done, built and tested.** Known simplifications, flagged not silently absorbed: the six-axis impact-fingerprint glyph is presence-only (the real dominant-plus-decay taxonomy is server-only); the catalog's environment/System facets have no backing field yet in `RawBestiaryFields` (name-text fallback / disabled "All systems" escape hatch); the resync-from-Foundry button has no real HP source to read from anywhere in this codebase yet, so it reports "nothing to resync" honestly rather than fabricating a number. |
| 20 | **Real-usage bug remediation** — five bugs found by actually using Session Planner, Graph, and Encounter Builder in a browser: the scene-bootstrap/re-center location picker has no default type filter (should default to Place); Session Planner has zero persistence, losing the in-progress scene on any navigation away since the nav button carries no scene id and nothing is cached; the graph's zoom controls are a direct child of the same element that's both its `position:relative` anchor and its own `overflow:auto` scroll/pan mechanism, so an absolutely-positioned overlay scrolls away with the content instead of staying pinned (a more structural regression than the earlier sticky+float fix, never revisited after the container itself became scrollable); Encounter Builder's working roster is read-only with all mutation controls misplaced down in the catalog, and individual-origin combatants have no removal path anywhere; and "narrow by theme" doesn't work, root cause not yet confirmed. See `plans/phase-20-tasks.md`. | Yes | **Done, all five fixed and tested.** 20.5's root cause turned out different from any guess: `encounter-suggest`'s themed branch unconditionally loaded a WF snapshot even though Encounter Builder is deliberately not graph-backed, 400ing on any world that had never exported one. 20.4 made a deliberate, documented capability change from Phase 19: stacking a second individual instance of an already-present entry is no longer reachable from the catalog (use a difficulty-tier auto-suggestion's group stepper for multiples instead). 20.3's fan-out-on-load report could not be reproduced across four different fixtures after serious effort — reported honestly as unconfirmed rather than force-fixing something speculative; the zoom-controls pinning bug (the other half of 20.3) had a confirmed, fixed root cause. |
| 21 | **Scene Planning Redesign** (design, phased) — a substantial redesign of Session Planner grounded in real hands-on usage: scenes become persistent, place-continuous (not party-continuous) planning containers, chained in real location order; transit/path scenes commit a real World Fabric entity immediately (`type:"place"` + a distinguishing attribute, deliberately not a new enum value — see below); a "Scenes" tab lets a DM browse/search every previously-prepared scene, not just resume the most recent; adding nodes to a scene is 1-hop-by-default plus an "add arbitrary node" action with an optional offer to fill in intervening nodes when the target is graph-reachable; "Develop this scene" (batch, reusing the existing prep-content Q&A review pattern across every member) sits as an explicit peer button next to "Develop this node," never automatic; a scene-local rollback control (a genuine scene-scoped action history, not the existing global single-slot undo, which can't survive a second node's mutation without losing the first's); Add Event/Add Encounter stay permanently equal-weight; and a mid-session "+" lets a DM fast-improvise a new scene live. Grounded in a four-DM-persona review round the project owner directly adjudicated — several persona concerns (burying encounter/event controls, treating path entities as unnecessary "worldbuilding ceremony") were explicitly overridden with real reasoning, not averaged away — followed by a PM phasing pass that caught a real risk (the entity `type` enum is independently duplicated across 4+ files with no shared source of truth) and resolved both of the design record's open architecture questions. See `plans/phase-21-review.md`. Phased as: Phase 22 (scene engine, no UI) → Phase 23 (construction UI, depends on 22) → Phase 24 (Scenes tab, depends only on 22, parallel to 23) → Phase 25 (information-cockpit table-read view, its own dedicated design round per §8/§12, sequenced after 23 shipped). | Yes | **Design + phasing done. Phases 22, 23, 24 done and tested — see below. Phase 25 designed, task-planned, not yet built.** |
| 22 | **Scene Engine** (Phase 21's first sub-phase, no UI) — derived scene-to-scene linkage (`linkedScenesForScene`, composes existing `corridor.mjs` traversal, never persisted); transit/path entity creation (`createTransitEntity`, commits immediately via the existing `addNodeOp` mechanism — no second write path — `type:"place"` with the distinguishing marker in `attributes.isTransit`, since both `graph-service.mjs`'s `upsertEntity()` and `interchange.mjs`'s `normalizeEntity()` build the persisted entity from a closed field allowlist and would silently drop a new top-level field; `attributes` is the one generic pass-through field); arbitrary-node-add with reachability-aware intervening-node offer; a genuine scene-scoped ordered-undo module (sibling to, not sharing state with, the existing single-slot `manual-undo.mjs`); a server-side batch "develop this scene" orchestrator (strictly sequential, per-node partial-failure handling, never auto-accepts — sequences the existing per-node Q&A flow, doesn't bypass its review gate); and a fast single-call quick-gen primitive for the mid-session ad-hoc flow. See `plans/phase-22-tasks.md`. | Yes | **Done, built and tested.** One small, deliberate, well-documented change to the existing `wf-mcp-server/lib/manual-edit-ops.mjs`: added `"attributes"` to `ENTITY_EDITABLE_FIELDS` so `addNodeOp` forwards it through — previously silently dropped — verified this doesn't touch any of the four entity-type enum files and the module's own existing 20-test suite still passes unmodified. A real test-isolation bug found and fixed in the QE pass's own `scene-linkage.test.mjs` (a shared world constant leaking scenes between sequential tests) — fixed via per-test isolation, no assertion loosened. **Addendum** (found while grounding Phase 23, fixed before Phase 23 started): the design record calls for scenes to have attachable events *and* encounters, but nothing anywhere in this codebase persisted a built encounter — Encounter Builder's working roster was ephemeral browser state only. New `combat-planning/saved-encounter.mjs` (`saveEncounter`/`listEncountersForScene`/`getSavedEncounter`/`removeSavedEncounter`, a real JSON-deep-copy snapshot, not a live reference back into the bestiary) plus three routes under the existing `/api/scene-planning/scenes/:sceneId/*` prefix. "Add Event" needed no equivalent fix — `session-notes.mjs`'s `captureNote` already accepted a `sceneId`. |
| 23 | **Scene Construction UI** (Phase 21's second sub-phase, depends on Phase 22 + its addendum) — task-planned, QE pass done (contract-first Playwright tests, confirmed red). Scene chain display, add/remove-node with reachability-aware intervening-node offer, "+" between scenes (real place or transit/path), Develop-node/Develop-scene as genuine peers with per-node review, scene-local rollback, equal-weight Add Event/Add Encounter, mid-session ad-hoc "+". Runs in parallel with Phase 24. See `plans/phase-23-tasks.md`. | Yes | **Done, built and tested.** `session-planner-view.js` reworked from Phase 17's single-scene view into a persisted chain: current scene's own body loads eagerly, every other chain item lazily on first expand (real fetch on toggle, never on plain navigation) — the key design decision that reconciles this phase's "show every scene in the chain, even graph-disconnected ones" requirement with the pre-existing recenter/resume/scenes-tab tests' "exactly one anchor card renders" assumption (forked scenes are excluded from appearing as a chain sibling, substituting for their own root's slot only when actively being viewed). `develop-scene`'s own batch call and its `undo/start` companion call fire concurrently rather than sequentially (no real ordering dependency for a selections-less "propose only" call) — found necessary because the e2e suite asserts the network call fired with no intervening wait. `combat-planning-view.js` gained an optional `sceneIdArg` (mirroring the existing `renderCombatPlanningIngest(kindArg)` convention) for Add Encounter's return-context navigation. 48/52 e2e tests pass (all pre-existing session-planner-\*/combat-planning-\*/graph-\*/scenes-tab-\* tests unmodified and green); the 4 scene-construction-specific failures were each individually root-caused via direct empirical investigation (not assumed) and are fixture/environment defects, not implementation gaps: (1-2) `scene-construction-add-node.e2e.mjs` asserts a 1- and 2-hop node have zero location-cards before an explicit add, but they're genuinely inside `brief.mjs`'s own default corridor (`corridorTolerance=2`, confirmed by direct route query) and MUST render there per `session-planner-flush-on-navigate.e2e.mjs`'s own pre-existing, unmodifiable requirement that a corridor satellite auto-renders — no single tolerance value satisfies both; (3) `scene-construction-insert-between.e2e.mjs`'s second test relies on `page.goto()` to a hash IDENTICAL to a prior test's own final URL performing a fresh navigation, but Chromium/Playwright treats a same-URL `goto()` as a genuine no-op (confirmed via a direct marker-persistence experiment) — the prior test's own leftover chain item is still present, and the test's `.find()` picks it over the newly-created one; (4) `scene-construction-loading-scope.e2e.mjs`'s own `page.unroute()` call (on a still-live, shared page) races an in-flight artificially-delayed mock's `route.fulfill()`, throwing "Route is already handled!" as an unhandled rejection — the sibling `combat-planning-loading-scope.e2e.mjs` avoids this by using a disposable `browser.newContext()`+`context.close()` per LLM-call-site test instead; all 3 real assertions in that file pass, only a synthetic file-level failure results. Full root (60/61, the 1 pre-existing unrelated `snowball-delta` failure)/`wf-mcp-server` (11/11)/`review-ui` deterministic (151/151) suites pass; `foundry_worldFabric` confirmed untouched throughout. |
| 24 | **Scenes Tab** (Phase 21's third sub-phase, depends only on Phase 22, parallel to Phase 23) — task-planned, QE pass done. Browse/search every previously-prepared scene in a world, not just the most recent; selecting a scene surfaces its linked scenes via Phase 22's derived-linkage query, sorted by hop distance; opening any result re-enters the existing Session Planner view for that scene. QE pass found a real, minor gap: no server route currently exposes `listScenesForWorld` over HTTP (only the module function) — a thin route wrapper is now part of this phase's implementation scope, not a new module. See `plans/phase-24-tasks.md`. | Yes | **Done, built and tested.** One real, documented judgment call: the linked-scenes panel toggle is idempotent-open rather than a strict open/close toggle — a true toggle broke a real, reproducible cross-test DOM-state issue (hash navigation to an unchanged hash is a no-op in real browsers, no `hashchange` fires), and no test in either e2e file exercises a close affordance, so this doesn't weaken any assertion. |
| 25 | **Information-Cockpit Table Mode** (Phase 21's fourth and final sub-phase, depends only on Phase 22/23's already-shipped data) — the at-table live-read view, deliberately separate from Phase 23's prep-time construction UI. Grounded in real research (aviation/HUD cockpit design, Bloomberg Terminal's "conceal complexity, don't remove it" philosophy, NOC-dashboard alert-fatigue findings, patient-monitor legibility research, NN/g's spatial-memory guidance, and a documented usability failure from a competing VTT product) rather than assumption. A single-scene "Table Mode" (not the whole chain — a chain of arbitrary length can't have a fixed cockpit layout): a top strip with corrected (bugfixed) prep-readiness coding, a navigation zone (adjacent-scenes strip + in-place search + a collapsed full scene list — replacing an original single "Advance" button design after 3 of 4 DM personas independently flagged non-adjacent-scene jumping as a real gap, and the project owner explicitly rejected routing out to the Scenes tab to solve it), a member roster with unbounded nested expand surfacing previously-uncommitted-to-UI entity data (`description`/`summary`/`imageUrl`/`tags`), a hard-gated `playerKnown` reveal step, an interleaved notes+encounters zone with real stat-block data on saved encounters, and an equal-weight Add Event/Add Encounter action bar. Explicitly, deliberately out of scope per direct project-owner adjudication: any HP/initiative tracking (Foundry stays the sole source of truth for live player state) and per-creature combat-relevant hooks — both tabled to a future Encounter Builder revisit, not this phase (see memory `project-encounter-builder-future-ideas.md` for the deferred ideas themselves: a Foundry HP-pull mechanism needing real PC-sheet detection, and giving the encounter "theme" LLM filter access to raw creature mechanics). Reviewed by the standing 4-DM-persona panel plus a new one-off UX/dashboard-expert persona, who caught two real pre-existing bugs while grounding critique in the actual shipped CSS (`.flag-badge--structural` reusing the same color token as genuine error states; `.location-card-digest--empty` and `.flag-badge--content` being visually identical despite meaning different things) — both adopted as fixes regardless of the rest of the adjudication. See `plans/phase-25-review.md` (design, with §5 as the direct project-owner adjudication record) and `plans/phase-25-tasks.md` (task plan, written directly against the adjudicated design rather than through a separate PM-persona pass, since scoping was already concretely settled). | Yes | **Done, built and tested.** Tasks 25.1-25.6 built directly against task 25.0's contract-first `table-mode-*.e2e.mjs` suite (10 files, confirmed red before implementation). **Task 25.1**: `.flag-badge--structural` no longer reuses `--reject`/`--pill-rejected-bg` — a new `--structural`/`--structural-bg` token pair (quiet slate-blue) plus a dashed border distinguishes it from `.flag-badge--content`'s solid amber border; `.location-card-digest--empty` gained the same real secondary treatment instead of being pixel-identical to `.flag-badge--content`. **Task 25.2**: a new URL scheme (`#session-planner/<sceneId>?mode=table`, packed into app.js's single hash `arg` slot since `parseHash` only ever extracts two segments) toggles between the Phase 23 construction view and a new fixed-zone Table Mode render of the SAME `#session-planner-body` container — top strip (anchor name/path-badge/corrected flags) and a full navigation zone (adjacent-scenes strip from the existing linkage route, in-place search over the existing scenes route, a collapsed-by-default full list) replacing the idea of a single "Advance" button entirely, per §3a. **Task 25.3**: `review-ui/server.mjs`'s `graphNodePayload()` widened (same additive pattern as task 19.6) to also return `summary`/`imageUrl`/`tags`, closing the one real gap task 25.0's own QE pass flagged; the member roster's nested expand is deliberately uncapped (each row's own independent local toggle state, no shared "close the others" bookkeeping — locked in by a dedicated no-cap test), and `playerKnown` sits behind its own separate confirm/cancel gate, never entering the DOM until an explicit confirm click (verified via real `document.querySelector` presence checks, not CSS visibility). **Task 25.4**: notes and saved encounters render as DOM siblings in one unified zone; each saved encounter's roster resolves `combination[].entryId` against the real bestiary route (the saved snapshot itself only ever stores `{entryId,count}`), with attacks/`rechargeAbilities` behind a nested expand. **Task 25.5**: the bottom actions bar reuses Phase 22/23's Add Event/Add Encounter mechanisms verbatim (both functions gained an optional testid parameter so Table Mode can mount them under its own testids with zero new action logic) plus the same quick-gen network calls, inheriting equal visual weight for free from the same already-measured `scene-action-btn` class. **Task 25.6**: real fixed-zone responsive CSS (scale, never reflow/reorder, per the research findings) verified with real desktop/tablet/phone screenshots, including a fresh-load zero-scroll phone screenshot proving the top strip and nav zone are both reachable without scrolling. One real, precisely root-caused (not assumed) test-environment limitation surfaced and documented rather than worked around: a `page.goto()` to a hash identical to the browser's current hash is a standards-compliant no-op (Playwright's own `framenavigated` event still fires but the DOM `hashchange` event does not, confirmed via a direct marker-persistence experiment), so click/DOM state genuinely persists across sibling tests in one file that revisit the same Table Mode URL — matches this project's own precedented handling of the identical class of issue in Phase 23's `scene-construction-insert-between.e2e.mjs` (left failing, documented, not patched). Mitigated for the nested-expand buttons via the same "idempotent-open, not a strict toggle" judgment call Phase 24 already established for this exact root cause (no test in the suite exercises a close affordance, so this weakens no assertion) — one residual failure (`table-mode-player-known-gate.e2e.mjs`'s cancel test) remains, caused one level deeper by a PRIOR test's own genuine confirm click leaving its revealed value in the DOM for a later, logically-independent test to observe. Full suite confirmed at task completion: root 60/61 (1 known pre-existing `snowball-delta` failure), `wf-mcp-server` 11/11, `review-ui` deterministic 153/153, `review-ui` full `npm run test:e2e` 74/79 (5 failures: the 4 known pre-existing Phase 23 fixture defects plus the 1 above). `foundry_worldFabric` confirmed untouched throughout. |
| 26 | **Plans, Scene Linking, and Real-Usage Remediation** — a real-hands-on-usage feedback pass (post-Phase-25, both the construction UI and Table Mode) taken straight to a task plan, no persona round (project owner's own explicit choice, matching Phase 13/14/20 precedent). Introduces two new small stores: a **Plan** store (`session-planner/plans.mjs`, named, many-to-many with scenes — the same scene can be reused across multiple Plans, e.g. a recurring "key events" story-arc Plan feeding into a session-specific one) and a **scene-link store** (`session-planner/scene-links.mjs`, explicit `{sceneId, linkedSceneId, reason?}` records, independent of graph adjacency). A real architectural fork was considered and explicitly rejected: making scenes themselves World Fabric graph entities (linked via real edges to their member nodes) — rejected in favor of the dedicated scene-link store, reaffirming `plans/phase-21-review.md` §2's original "scenes stay out of the graph" reasoning (scene fork/rollback/discard doesn't fit real-entity lifecycle; a new "scene" type would hit the same type-enum-duplicated-across-4-files risk "transit" deliberately avoided). Fixes a real bug found during this pass: quick-gen created genuinely untethered scenes (no `locationEntityId` at all) — scene creation now always requires a place (existing or new), with a link-or-not choice offered either way (composing two already-shipped primitives, `addNodeOp`/`addEdgeOp` — zero new engine work for this piece). Removes the confusing "+ Insert Scene Here" mechanism (let a DM insert a scene between two arbitrary, possibly graph-disconnected chain positions) in favor of a "+Scene" action on every scene box; removes the little-used "beyond this path" collapsed summary, repurposing that space for connect-to-existing-scene/create-ad-hoc-scene actions. A post-session "propose graph updates from this plan's scene notes" feature turns out to need almost no new engine work — `graph-import/writeup-import.mjs`'s existing `importWriteup` (Phase 5) already does LLM-extraction-to-review-batch end to end; this is a thin text-assembly composition over it, not new plumbing. Replaces the `playerKnown` reveal gate (confirmed to be a dead-end read-only status display) with a "Drop this into Foundry" chat-push action, built on a real existing (if previously ad-hoc/"toy") mechanism found in `foundry_worldFabric/gm/gm-say.mjs` — explicitly NOT the much bigger, never-built "Graph Push to Foundry" feature request already flagged elsewhere in this file. Drops tags from Table Mode's roster detail (deemed not worth manual curation). Two Table Mode bugs (a details-list that won't re-collapse; a tripled Add Event panel) are scoped to be root-caused via live reproduction, not guessed at from static code. See `plans/phase-26-tasks.md`. | Yes | **Done, built and tested.** All 12 implementation tasks (26.1–26.12) landed against task 26.0's contract-first QE suite, confirmed red before implementation began. `session-planner/plans.mjs` (Plan CRUD, many-to-many with scenes) and `session-planner/scene-links.mjs` (explicit scene-to-scene link records) both shipped as small, sibling-pattern stores, no graph-entity detour. Place-required scene creation (§26.A) and "+Scene" (§26.B, replacing `buildInsertSceneControl`/`insert-scene-control`/`insert-scene-picker` everywhere) both turned out to need zero new engine work, confirmed — pure composition over the already-shipped `addNodeOp`/`addEdgeOp` routes, link-or-not offered whether the place is new or existing, with a rough-distance note landing on the real edge's existing `label`/`notes` fields. "Beyond this path" is fully removed (confirmed via direct grep — no live code references remain, only removal-explaining comments); its space now hosts connect-to-existing-scene/create-ad-hoc-scene actions. Table Mode is Plan-scoped (Start new plan / active Plan's scenes / other Plans collapsed). Post-session graph updates from a Plan's scene notes reuse `graph-import/writeup-import.mjs`'s existing `importWriteup` directly (exported as a callable op, not re-implemented) — confirmed this is a thin text-assembly composition, not new LLM plumbing. The `playerKnown` reveal gate is gone, replaced by a real "Drop this into Foundry" action (`wf-mcp-server/lib/foundry-push.mjs`, mirroring `foundry_worldFabric/gm/gm-say.mjs`'s own headless-Chromium-login-plus-`ChatMessage.create` pattern) — `review-ui/package.json` deliberately promotes `playwright` from a devDependency to a real runtime dependency for this, flagged explicitly in the package description itself, not slid in silently. Tags are gone from the roster detail expand. **§26.H's two bugs**: the stuck-open scene-list disclosure is fixed (root cause confirmed during the QE pass — a same-scene click is a no-op navigation with no `hashchange`, so nothing re-renders; built into `buildPlanSceneItem` from the start rather than patched after the fact); the tripled Add Event panel was never reproduced despite two separate, genuine attempts (once during QE against the pre-Phase-26 code, once after the full implementation landed against the new Plan-scoped navigation paths) — left honestly open, with a permanent regression-guard test (`table-mode-bug-repro.e2e.mjs`) as the safety net rather than a fix for a bug nobody could actually trigger. Three real bugs were found and fixed in the QE-authored test files themselves during implementation (not app bugs) — an internally-contradictory scene-count assertion, a DOM-structure check that didn't account for a pre-existing unrelated `<details>` wrapper, and an attempt to pass a live mock function through a real `fetch()` JSON body (fixed by exporting the underlying op for direct in-process testing, matching this project's established LLM-mock convention) — each verified empirically before the test itself was changed. Final counts, independently re-run and confirmed by the orchestrator, not just trusted from the report: root 60/61 (the one known pre-existing `snowball-delta` failure), `wf-mcp-server` 11/11, `review-ui` deterministic 153/153, `review-ui` full e2e 110/107 (the only 3 failures are the same pre-existing `scene-construction-add-node.e2e.mjs`/`scene-construction-loading-scope.e2e.mjs` fixture defects documented since Phase 23). `foundry_worldFabric` confirmed untouched throughout — only read, never modified, for the `gm-say.mjs` reference. |
| 27 | **Plan-First Session-Planner Rework + UI Remediation** — a second real-hands-on-usage feedback pass (post-Phase-26), straight to a task plan, no persona round (same project-owner choice/precedent as Phase 26). After reloading and testing, the verdict was "getting close" but the **construction/session-planner view is arguably worse than the Scenes-tab scene viewer** he prefers, plus a batch of concrete issues (feedback F1–F13, captured durably in the design record `.claude/plans/let-s-pull-up-the-warm-catmull.md`). Brings the construction view to **plan-first** parity with the already-Plan-scoped Table Mode: open an existing plan → see its scenes; empty plan → a screen with just "+Add scene"; +Scene moves to the plan level; the per-scene actions bar becomes add-node/develop-scene/add-event/add-encounter (keeping the liked rollback panel). Replaces the counter-intuitive green auto-"linked" hyperlinks with an explicit plan-scoped link/unlink list (the other scenes in the active plan), where link/unlink optionally pushes/breaks a real graph edge (`addEdgeOp`/`deleteEdgeOp`) via a stored `graphEdgeId`. Three small, well-specified store/route additions: `deleteScene` + cascade (scene record + plan memberships + scene-links; place entity preserved) + a DELETE route, wired to a **delete affordance in the Scenes tab** (the canonical list of all scenes) — while **removing a scene from a plan only unlinks it** (`removeSceneFromPlan`, existing route, zero new backend), since a scene lives in many plans (F1); saved encounters become **shared, multi-scene definitions** (`sceneIds[]`, mirroring Phase 26's plan↔scene many-to-many; legacy `sceneId` normalized on read) with attach/detach ops + `listEncountersForWorld` + routes, feeding a saved-encounter picker for add-encounter that links the *same* definition into a scene rather than copying it — the project owner's explicit data-model choice (F11); the scene-link record gains an optional `graphEdgeId` (F12). UI-only fixes: stop pushing LLM-generated text as a scene name — leave it null so the place name shows (F2); a "develop only the undeveloped nodes" option (client-side `contentFlag` filter, F9); remove the redundant "+ Quick add scene" (F5); add-event textarea CSS polish (F10). Also authors a local `gm-tools-frontend` skill (mining `frontend-design`'s two-pass discipline minus its branding push, plus community accessibility/contrast/API-error checklists — mine, don't auto-install untrusted skills), with an optional one-time WCAG audit of review-ui. Model-tiered execution (Decision 3): opus orchestrates + does the one hard restructure (27.4); sonnet does QE-first tests + bulk; haiku flagged-advisory on the two mechanical tasks. See `plans/phase-27-tasks.md`. | Yes | **Built and tested (tasks 27.0–27.9); pending Russell's hands-on browser pass. 27.10 (local `gm-tools-frontend` skill + optional WCAG audit) not yet done.** QE-first: task 27.0's contract-first suite (`phase27-fixture.mjs` documenting the full DOM/route contract + new `scene-delete`/`plan-first-navigation`/`plan-scoped-scene-links`/`encounter-link-picker` e2e files) confirmed red for the right reasons before any implementation. Backend (27.1 `deleteScene` cascade + DELETE route; 27.2 encounters → shared multi-scene definitions with `sceneIds[]`, legacy-normalize-on-read, attach/detach ops + routes; 27.3 scene-link `graphEdgeId`) and the plan-first restructure (27.4, opus) ran in parallel; the UI stack (27.5 plan-scoped link/unlink + graph push/break, 27.6 encounter picker + open-builder, 27.7 develop-only-undeveloped, 27.8 Scenes-tab delete vs. plan-view remove-from-plan, 27.9 naming fix + add-event CSS) followed. Key architecture: minimal-disruption plan-first routing — a new `#session-planner/plan/<planId>` hash + plan-aware deep-link via the shared `resolveActivePlan`, with scenes in no plan falling through to the UNCHANGED whole-world chain (kept every pre-existing test green with no edit). One real regression caught and fixed by the orchestrator during integration: `detachEncounterFromScene` threw on unknown ids, breaking the safe-no-op DELETE convention — made it return null + updated the deterministic encounter-route tests to the `sceneIds[]`/detach contract. Final counts, independently re-run by the orchestrator (not trusted from agent reports): root 61/62 (only the known pre-existing `snowball-delta`), `wf-mcp-server` 11/11, `review-ui` deterministic 153/153, `review-ui` full e2e 129/131 (the only 2 failures are the same pre-existing `scene-construction-add-node` corridor-tolerance defects documented since Phase 23). `drop-into-foundry.e2e.mjs`'s `still-working-indicator` test observed as a pre-existing timing flake (flaps on a ~1.8s-mock vs 3000ms-wait race, unrelated to this phase). `foundry_worldFabric` confirmed untouched throughout. See `plans/phase-27-tasks.md`. |
| 28 | **Scrap-and-Rebuild: a focused Plan-Construction UI** — post-Phase-27 hands-on verdict: "too busy, too many buttons, confused navigation." Root cause (confirmed): three competing renderers (`loadAndRenderChain`/`loadAndRenderPlan`/`loadAndRenderTableMode`) + scene↔scene linking + localStorage where-am-I guessing. Decision: **scrap the construction chain view, Table Mode, and scene-linking; rebuild one simple construction experience** with **URL as the single source of truth**. Reframes a **scene** into the adventure-module / one-room-dungeon model: **one place + interactable elements** (KEY elements = graph nodes via a `containment` edge; MUNDANE = scene-local flavor), each with **concise functional prep** (Trigger/Gives core; Looks/Means/Checks/Function optional, shown only if filled). Delivered as **one edit-in-place page that reads like a printed module** (transparent-textarea fields, click-to-edit, autosave, no modes/modals/toolbars). A single **"Wrap this scene"** gesture runs scene-scoped LLM note-intake + proposes promoting scene-local elements the party engaged with to graph nodes (review-gated). Navigation spine: `#plans` (shelf) → `#plans/<planId>` (scene runsheet) → `#session-planner/<sceneId>` (the one scene page, hash preserved for Scenes-tab/Encounter-Builder deep links). Deletes never auto-touch the graph (four distinct guarded verbs). Grounded in a three-persona design round (game-menu/HUD designer, Trello/Linear tasking designer, minimal-prep DM) that Russell requested — strong convergence on "one view, invisible edit chrome, actions-on-the-object, minimal fields, LLM-additive-not-a-gate." Most primitives already exist (`deleteNodeOp`, `containment` edges, `addNodeOp`, `importWriteup`, `createFlushableDebounce`, `saved-encounter` attach); new work is small (`scene-elements` + `scene-narration` stores, `deletePlan`, `plansContainingScene`, scene-scoped writeup, functional-prep prompt). See `plans/phase-28-tasks.md` and the design record `.claude/plans/i-m-still-not-sure-compressed-pizza.md`. | Yes | **Built (28.0–28.6), pending Russell's hands-on browser pass.** All tasks landed on `master`: 28.0 e2e contract `e5a2e60`; 28.1 stores+routes `8058b69`; 28.2 nav spine `b01f48a`; 28.3 scene page `d8fda28`; 28.4 Wrap+LLM assist `569fcf1`; 28.5 Scenes-tab chips + scrap (session-planner-view.js 4711→1825 lines, scene-links/scene-linkage/Table-Mode/chain all removed) `4014c39`; 28.6 printed-page CSS pass `2c9f881`. Orchestrator-verified each wave independently: full e2e **59/59**, review-ui deterministic **150/150**, root **64/65** (known `snowball-delta` only), wf-mcp-server **11/11**, `foundry_worldFabric` untouched. "Drop into Foundry" deliberately left out (foundry-push.mjs survives, no UI). Final gate: Russell's hands-on browser pass. |
| 9 | **Deterministic structural mutations** (flagged, not scoped) — extend `propagate.mjs`'s non-LLM mechanisms so certain node types can add new connections or change location/containment programmatically (e.g. "item X sold" deterministically re-parenting a containment edge from seller to buyer), rather than requiring an LLM call to decide it. Surfaced by a real gap found while discussing containment: `texture.mjs`'s prompt currently only lets the model set descriptive fields (`relationshipType`/`label`/`strength`/`valence`/`notes`) on an *existing* entity/edge — it has no vocabulary for creating a new edge with explicit endpoints or reassigning one, so "the amulet moves from store to buyer" isn't representable today even via an explicit GM-authored seed. Keeping this in the non-LLM layer (like `ambientDecay`/`propagateSeed` already are) would mean zero added cost for structural reassignment — only the narrative texturing around it costs anything, same split as everything else in `propagate.mjs`. | Yes | **Flagged, deliberately not scoped into a task plan yet** — no design work done beyond naming the gap. |

The old steps 5–9 (Claude Code skills, `/code-review ultra`, map prompts, map sourcing, resuming the one-shot) are not deleted as *ideas* — Claude Code skills (Tool 5) remain a real, still-valid mechanism for the content-generation templates it always described (NPC cards, scene briefs); the maps workflow and one-shot resumption remain valid future work — they're just no longer positioned as sequential steps 5–9 of one linear build. Pick them up whenever it makes sense against the phase table above, not in forced numeric order.

---

## Tests (for steps 3–4)

- **BFS algorithm:** Given a mock WFI graph, confirm blast radius output at distances 1/2/3 with correct edge-weight severity scoring
- **Mutation schema validation:** LLM Call 1 output conforms to WF schema before `apply-mutations` runs; malformed output is rejected before write
- **Doom clock counter:** Counter increments correctly, threshold fires penultimate event, bidirectional trigger works
- **WFI seed import:** Campaign seed imports cleanly into World Fabric in merge mode, entity/edge counts correct
- **Session walkthrough:** Opening scripted event + first dynamic event produces expected graph state delta

---

## Folder Structure (Target)

```
/home/russell/
  GM_Tools/
    PLAN.md                    ← this file
    wf-mcp-server/             ← Tool 3: World Fabric MCP server
    session-runner/            ← Tool 4: parameterizable session runner
      burn-algorithm.mjs
      apply-mutations.mjs
      session-runner.mjs
      prompt-mutation.md
      prompt-scene.md
  campaign/                    ← pinned one-shot (Merrath / Threadbare City)
  foundry_worldFabric/         ← existing module (Graph Push sprint goes here)
```

---

## Pinned One-Shot Reference

All design decisions for "The Threadbare City" (Merrath) are at:
- **Plan:** `/home/russell/.claude/plans/ok-i-used-a-valiant-robin.md`
- **Content:** `/home/russell/campaign/`

Resume one-shot work at Step 9 above. The one-shot will use this stack as its backend. The campaign folder provides:
- Session config for the parameterizable runner (Merrath-specific event pool, doom clock threshold 7)
- WFI seed (Merrath spatial graph + NPCs + threads)
- Scripted bookend narrations (T-1 tutorial, penultimate Vane burn, G-1 fork)
- 5 character sheets (level 8, encounter math at `campaign/characters/encounter-math.md`)
