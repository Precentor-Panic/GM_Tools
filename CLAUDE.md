# GM Tools — Orientation

Read this first. It's the front door — point a fresh session at `GM_Tools/` and this file plus the two it links to should be enough to pick the work back up with no other context.

## What this is

A reusable stack for LLM-assisted TTRPG session design and live play, built on top of **World Fabric** (`/home/russell/foundry_worldFabric`, a Foundry VTT module that stores campaign lore as a knowledge graph). Generalized from "The Threadbare City" one-shot, but not tied to it or to D&D.

**The core pattern, everything else is built around this:**
```
event fires at a graph node
  → BFS outward: (node_id, distance, path_edge_type)[]        [code, not LLM]
  → LLM call [mutation]: event semantics × blast radius → structured graph mutations
  → apply mutations to graph                                   [code, not LLM]
  → LLM call [scene]: updated subgraph snapshot → scene/consequence description
```
Full LLM-vs-code task breakdown is in `PLAN.md`.

## Current status — always check `PLAN.md` § Build Sequence for the live table

Don't trust a summary of status here or in memory over that table; it's the thing kept current. As of the last update: **Graph Push** (bulk push WF entities → Foundry docs) and the **WF MCP server** are built. The generic Foundry actor/scene MCP is configured but needs approval. The linear "session runner" concept from the original plan has been superseded by a phased toolbox approach — see `PLAN.md` § Build Sequence and `plans/phase-1-tasks.md` for current status; **Phase 1 (mutation engine core) is done** (tasks 1.1–1.8; task 1.9's web UI stays deferred to Phase 6). **Phase 2/2b (time-skip mode + headless apply) is done** — `time-skip/`, `graph-import/` built and tested per `plans/phase-2-tasks.md`; the two real-API smoke tests (resumability cost-reduction, LLM-inferred seed resolution) are written but not executed — no `ANTHROPIC_API_KEY` in the build environment. **Phase 3 (live on-demand diff mode) is done** — the scene-narration LLM call (`mutation-engine/narrate.mjs`) is built, gated to accepted batches only, and wired as `wf_narrate_batch`; real latency measurement showed `wf_propose_mutations` is not reliably sub-5s live (p50 ~51-59s) regardless of BFS depth, so the MCP tool description now sets that expectation rather than the code pretending otherwise. See `plans/phase-3-tasks.md`. **Phase 3.5 (deferred/lazy consequence resolution) is done** — a new pending-resolution ledger (`mutation-engine/pending-ledger.mjs`) lets large time-skips (`wf_run_cycle`) eagerly texture only a per-cycle "headline" focus and defer everything else in scope as a cheap ledger tag, resolved later only on explicit request (`wf_resolve_pending`), with a fan-out cap bounding cost. See `plans/phase-3.5-tasks.md` and `PLAN.md`'s Phase 3.5 row for the full build summary. **Phase 4 (rollback hardening + unreviewed-accumulation tracking) is done** — the newly-created-entity rollback gap is closed for the headless path (`graph-import/headless-apply.mjs` now reports back assigned ids, written onto the batch by `wf_sync_to_foundry`), the live-Foundry path is confirmed unclosable from this side (documented, not left unexamined), and a new `mutation-engine/human-review.mjs` tracks per-entity `lastHumanReviewedAt` — updated only by genuine scoped review actions, never by a whole-batch accept-all — surfaced via the new `wf_get_unreviewed_entities` tool and forced into `wf_review_batch`'s headline rendering regardless of importance. See `plans/phase-4-tasks.md` and `PLAN.md`'s Phase 4 row for the full build summary. **Phase 5 (import-from-writeup) is done** — `graph-import/writeup-import.mjs` turns freeform text into a WFI-shaped proposal via one LLM call, dry-runs it through World Fabric's existing `importGraph` merge/dedup logic without persisting, and converts the result into a normal review batch (`wf_propose_from_writeup`) that goes through the exact same review/accept/sync pipeline as every other mutation batch. A real, API-backed round trip confirmed both the dedup-not-duplicate case (an existing entity merges instead of creating a second one) and the bootstrap-from-nothing case (a freshly-bootstrapped empty campaign). See `plans/phase-5-tasks.md` and `PLAN.md`'s Phase 5 row for the full build summary, including a real cross-mutation id-stability bug found and fixed via that same smoke test. **Phase 6 (dedicated web review UI) is done** — `review-ui/server.mjs` (a small, dependency-free `node:http` server) plus a vanilla HTML/CSS/JS frontend (`review-ui/public/`) give the project its first actual visual surface: a pending-work Queue, a Batch Review screen with per-mutation accept/reject/regenerate/narrate and the narration-vs-rationale visual distinction, a Deferred Debt tab, and Settings (sync status, one-shot rollback, a stubbed graph-browser link). `wf-mcp-server`'s review-workflow tool handlers were refactored (no behavior change, full test suite reconfirmed) into a new shared `wf-mcp-server/lib/mutation-ops.mjs` so review-ui reuses the exact same code instead of a second copy. Actually visually verified via a real headless-Chromium run (no interactive browser was available in this environment) — see `plans/phase-6-tasks.md` and `PLAN.md`'s Phase 6 row for the full build summary, including a real rendering bug found and fixed only because of that visual check. **First real hands-on use of the review UI (post-Phase-8) surfaced a real, high-severity bug**: accepting every mutation in a batch only changes review-state status — it never actually writes anything to the World Fabric graph, and no button anywhere in review-ui ever called the existing sync route. Fixed, along with a stale-flag-until-reload bug and several smaller UX gaps (no Select All/None, unclear "boring" terminology, no plain-language "what did this batch do" explainer). Also root-caused a cluster of narration complaints (identical text across every row, "session zero" framing, nothing persisting across page loads) to whole-batch-only narration with zero grounding context ever passed — real design work, not a quick patch, scoped and now **fixed in Phase 10** (see its own paragraph below for the full build summary). A related new idea — per-node content generation (description/actions/secrets/rolls) — was flagged as its own **Phase 11**, deliberately kept separate from Phase 7, and is now done (see its own paragraph below).

**Phase 8 (rubber-duck creative mode) is done** — tasks 8.1–8.5 built and tested per `plans/phase-8-tasks.md`, design followed from `plans/phase-8-review.md` as written. An opt-in global setting (`mutation-engine/user-settings.mjs`, `human-review.mjs`'s flat-JSON convention) sits in front of Phase 5's writeup-import pipeline: when on, submitting a writeup first returns three cheap, deliberately fast one-sentence interpretive framings (`graph-import/writeup-import.mjs`'s `proposeFramingsFromWriteup`, `claude-haiku-4-5` — measured ~5-25s versus the real extraction's ~51-59s p50 baseline) instead of immediately running the real extraction; picking (or blending) one steers the real pass via the existing regenerate-with-note mechanism. The reject-loop is resolved exactly as designed: a plain reject loops back to one bounded round of new, informed framings (`FramingRoundLimitError` refuses a second one, requiring an explicit note instead); an explicit-note reject always skips the loop entirely, straight to the existing `regenerateWriteupImport`. The setting is read from `mutation-engine/user-settings.mjs` **exactly once**, at writeup-submission time, and snapshotted onto `batch.scope.rubberDuck` — every later action reads the batch's own stamped snapshot, confirmed by grep that no other call site re-reads the live setting, so a mid-review toggle flip can't retroactively change an in-flight batch; rubber-duck-off is a confirmed byte-identical pass-through to Phase 5 (that phase's own smoke test re-run, unmodified, still passes). New MCP tools `wf_select_framing`/`wf_get_rubber_duck_mode`/`wf_set_rubber_duck_mode`; `wf_reject` gained optional `note`/`quickPickReason` params. review-ui gained real new frontend surface — a "New Import" paste screen (it had none before), a "First Reactions" framing-selection screen, a reject-loop quick-pick panel on Batch Review, and a Settings toggle — actually visually verified via real headless Chromium (no interactive browser was available in this environment, same as Phase 6) driving a real, API-backed click-through of the entire flow end-to-end, 15+ screenshots inspected. See `PLAN.md`'s Phase 8 row for the full build summary, including a real defense-in-depth gap found and fixed in the round-bound enforcement during self-review.

**Phase 10 (per-entity narration & persistence) is done** — tasks 10.1–10.5 built and tested per `plans/phase-10-tasks.md`, design followed from `plans/phase-10-review.md` as written, no redesign. This directly fixes the narration bug cluster described above. New `mutation-engine/entity-narration.mjs` (task 10.1) is a genuine per-`(world,entityId)` HISTORY store (`human-review.mjs`'s flat-JSON/env-override/`withLock` convention, `pending-ledger.mjs`'s per-entity-file layout) — nothing is ever deleted, only marked `'superseded'` when a newer narration is saved or the entity is mutated again. `mutation-engine/narrate.mjs` gained `narrateEntity()` (task 10.2): the same truncation-retry handling as the original `narrateBatch`, but gated at ENTITY grain (a sibling mutation's status never blocks narrating this one) and targeted using the entity's own real immediate graph neighbors (`wf-mcp-server/lib/graph.mjs`'s `neighborhood()`, the same primitive `time-skip/resolve-pending.mjs` already reuses) instead of the always-empty location/area slots that caused the reported generic framing — a real-API smoke test (`mutation-engine/narrate-entity.smoke.mjs`) proved two entities in one batch now get genuinely different, well-grounded narrations. Task 10.3 wired supersede-on-re-mutation into `wf-mcp-server/lib/mutation-ops.mjs`'s single shared accept choke point. Task 10.4 added three new MCP tools (`wf_narrate_entity`/`wf_get_entity_narration`/`wf_get_entity_narration_history`) and matching review-ui routes. Task 10.5 replaced review-ui's old whole-batch, in-memory-only narration cache with a per-row fetch/render of that entity's own durable current narration plus a "View narration history" affordance — actually visually verified via real headless Chromium (no interactive browser was available in this environment, same as every prior UI phase), proving both of the concrete required behaviors: two different rows in one batch produce genuinely different text, and a completely fresh page load shows both previously-generated narrations already in place rather than offering to generate new ones. A self-review remediation pass found and fixed one real gap: `buildAdjacencyContext` had no handling at all for an edge (`upsert_edge`/`delete_edge`) mutation — a common, ordinary case, not a rare corner — and silently degraded to grounding on the raw internal edge id with zero real neighbors; fixed to ground on the edge's own two real endpoints instead. See `PLAN.md`'s Phase 10 row for the full build summary.

**Phase 7 (Foundry-optional visual graph view) is done** — tasks 7.1–7.4 built and tested per `plans/phase-7-tasks.md`, design followed from `plans/phase-7-review.md` as written, no redesign. New `GET /api/graph` in `review-ui/server.mjs` (task 7.1) is a thin composition over existing primitives only — `wf-mcp-server/lib/graph.mjs`'s `neighborhood()`, `mutation-ops.mjs`'s `flaggedEntityIdSet`, `pending-ledger.mjs`'s `listPendingEntities` — never a second traversal/status implementation; batch-scoped mode returns a batch's own proposed entities/edges plus their persisted one-hop neighbors, standalone mode defaults (when `filter` is omitted) to the confirmed unreviewed-OR-deferred-debt filter, `filter=all` being the explicit "show everything" escape hatch. New `review-ui/public/graph-view.js` (task 7.2) is a single shared vanilla-SVG rendering module — no charting library, a compact hand-rolled force-directed layout with positions cached in `localStorage` so reopening a graph never rearranges it and this survives a real page reload — used by both a new `[List | Graph]` toggle on Batch Review (task 7.3, List default) and a new standalone "Graph" nav view (task 7.4). The single most important correctness point: Graph mode shares the EXACT SAME checkboxes and accept/reject functions List mode already uses (never a second selection/accept-reject state) — verified against the real API, not just visually, that accept-via-popover and shift-click-then-bulk-accept both produce the identical on-disk mutation status a List-mode action would. Unreviewed (amber border) and deferred-debt (corner badge) render as two independent, co-occurring visual channels, confirmed by a real screenshot of a node showing both at once. Actually visually verified via real headless Chromium (no interactive browser was available in this environment, same as every prior UI phase) against a hand-built fixture, including the standalone view's default fetch genuinely returning fewer nodes than `filter=all` from the real network response, and layout positions staying byte-identical across two consecutive real page reloads. A self-review remediation pass found and fixed two related real bugs surfaced only by continuing to drive the app: a `window`-level event-listener leak in the rubber-band-select code (rebound on every re-render), and a regression that same fix introduced — moving the drag-start listener onto the graph's container element caused a mousedown on the popover's own Accept/Reject button to bubble into it and close the popover before the click could register, silently breaking Accept/Reject from the graph popover entirely — caught by watching for the (missing) network request, fixed with the same stopPropagation-on-mousedown convention graph nodes already used, and re-verified end to end against the real API afterward. See `PLAN.md`'s Phase 7 row for the full build summary.

**Phase 11 (per-node content generation, "develop this node") is done** — tasks 11.1–11.5 built and tested per `plans/phase-11-tasks.md`, design followed from `plans/phase-11-review.md` as written, no redesign. A new `mutation-engine/prep-content.mjs` (task 11.1) is a genuinely separate store from the mutation-engine/review-state.mjs pipeline — verified by directly reading `schema.mjs`'s `Mutation`/`StoredMutation` shape (no `op`/`batchId`, never diffed against the live graph, never Foundry-synced), not just trusted from the design doc's own flagged caveat — following `entity-narration.mjs`/`human-review.mjs`'s exact flat-JSON/`withLock`/env-override convention, all six entity-type templates implemented per the design doc's field lists. Task 11.2's framing-then-generation calls mirror the whole-writeup rubber-duck mechanism one level down to a single entity, both grounded in the entity's real immediate graph neighborhood (reusing `narrate.mjs`'s `buildAdjacencyContext`) — a real-API smoke test proved a person and a place produce genuinely different, type-appropriate, graph-grounded field shapes, the actual regression guard for "generic mushy content forced onto everything." Task 11.3 added a shared `wf-mcp-server/lib/prep-content-ops.mjs`, 8 new MCP tools, and matching review-ui routes, deliberately carrying no `batchId` parameter anywhere. Task 11.4 wired staleness-on-remutation into `wf-mcp-server/lib/mutation-ops.mjs`'s `acceptMutationIds` — the SAME choke point Phase 10 already used for narration invalidation. Task 11.5 added a new entity-detail view reachable only from the standalone Graph view's node popover (a "Develop this node →" link, structurally absent from Batch Review's List/Graph toggle) implementing the full propose→frame→generate→accept→per-field-regenerate flow with a quiet staleness badge. Actually visually verified via real headless Chromium (no interactive browser was available in this environment, same as every prior UI phase), 22 screenshots confirmed every required behavior, including that "Develop this node" never appears anywhere in Batch Review. A self-review remediation pass re-confirmed by direct grep that PrepContent never reaches any Foundry-sync code path, no bulk "develop all" affordance exists anywhere, and staleness only ever flips a status flag, never deletes content. See `PLAN.md`'s Phase 11 row for the full build summary.

**Second real hands-on session (post-Phase-11) surfaced more real gaps, fixed directly**: the graph view's fixed 420px box had no zoom/pan (added, deliberately as a CSS size change inside a scrollable container rather than a viewBox/transform change, so the existing screen↔layout coordinate math in `showPopover()`/`wireRubberBandSelection()` needed zero changes and stayed correct at any zoom level — verified directly against both at 150% zoom); the standalone Graph view defaulted to flagged-only and required re-clicking "Show everything" on every visit (flipped, both server- and client-side); a fully-rejected batch never left the Queue's "Awaiting review" list since `batch.status` only ever transitions on sync/rollback, never on reject-everything (fixed — `listBatches()` now reports `acceptedCount`, and the Queue filters on "still has a pending decision or accepted-but-unsynced work"); and the "Long-unreviewed entities" list was read-only, with no way to dismiss a flag for an entity with no currently-open batch (fixed — a new standalone mark-reviewed route + a "Mark Reviewed" button). Also surfaced, investigated but not unilaterally acted on: real stray entities (`alvor`/`riverwood`/`gerdur`/`sven`) sitting in the real `wf-test` world with no way to remove them — plausibly pre-existing World Fabric demo/seed content given the world's name and those exact names being Skyrim's opening-village NPCs, not confirmed either way, nothing deleted without the project owner's say-so. This directly motivated **Phase 12 (interactive graph editor)**, now done — see its own paragraph below.

**Phase 12 (interactive graph editor) is done** — tasks 12.1–12.6 built and tested per `plans/phase-12-tasks.md`, design followed from `plans/phase-12-review.md` as written, no redesign. This is the project's first genuine additive change to the separate `foundry_worldFabric` repo: four new plain scalar entity fields (`status`/`playerKnown`/`canonLocked`/`role`, confirmed by the project owner) wired into both the live (`graph-service.mjs`) and headless (`interchange.mjs`) write paths, plus a new, exported `migrate-entity-fields.mjs` backfill step (wired into `module.mjs`'s `_runMigration`) for entities persisted before these fields existed — required because an additive field-list change alone never reaches an already-persisted entity record, the exact class of gap this project hit twice before for entityTypes' attributeDefs (Phase 1.5b/1.5c). **Live-reverified against the real `wf-test` world's own exported snapshot**, not a synthetic fixture: all 18 real entities, including `alvor`/`riverwood`/`gerdur`/`sven`, predated the four fields and backfill cleanly and idempotently. `foundry_worldFabric`'s own pre-existing unrelated uncommitted changes were confirmed untouched throughout, and this repo's changes were committed as their own separate history there. Manual node/edge create/edit/delete (task 12.3) write immediately with no review gate — the GM directly authoring, not reviewing an AI proposal — reusing the exact same live-then-headless apply path every other write in this project already uses, with every create pre-assigning its own id so the well-known "live path can't report a created id back" gap never applies. The single-slot "Undo Last Manual Edit" mechanism (task 12.4, flagged by the design review as the single most important piece of this phase) deliberately mirrors `rollback.mjs`'s own simplicity and covers all six manual-write kinds plus narration reset, with a node delete's cascade (the node plus every edge it touches) captured and restored as ONE atomic action — proven by a 19-case direct-call test suite, not just reasoned about. "Scan for mentioned entities" (task 12.5) reuses writeup-import's real name+type dedup (not a second matcher) and produces genuinely different mutation shapes for a link (edge-only) versus a proposed-new entity (entity+edge) — confirmed by a dedicated unit test, not cosmetic labeling — and still routes through the existing accept/reject review flow unchanged. Narration reset (task 12.6) appends a new empty version to the existing per-entity history (never deletes, per Phase 10's established convention) and is itself undoable via a further append. None of this new manual-edit surface is exposed as an MCP tool — an LLM-driven call reaching that same unreviewed write path would undermine the no-silent-auto-write invariant this whole project is built around. Actually visually verified via real headless Chromium (no interactive browser available in this environment, same as every prior UI phase): placing a node at the clicked point, drawing an edge with the type prompt opening immediately, editing a node's fields directly, deleting a node with the real cascade count shown and both edges coming back atomically via Undo, the two visually-distinguishable scan-mention badges with correctly-counted split accept-all buttons, and a reset followed by confirming the original text is still visible in history — zero console errors, dark mode confirmed separately. Three real DOM-event-ordering bugs (invisible to unit/route tests, since none has a server-side symptom) were found and fixed only by actually driving this in a browser: a placement click landing on an existing node also triggered that node's own popover afterward and destroyed the just-opened create form; a popover button that mutates its own DOM mid-click (the delete confirm) detached the click's target from the popover before the outside-click dismiss listener checked it, closing the popover out from under its own confirm UI (fixed by switching to `evt.composedPath()`, captured at dispatch time); and a plain scoping `ReferenceError`. See `plans/phase-12-review.md` and `plans/phase-12-tasks.md`, and `PLAN.md`'s Phase 12 row for the full build summary. Also fixed directly, from a follow-up hands-on check: every manual edit's own "still working" latency (the same ~7s dual-path apply as Sync) now shows a "Still working…" message on all five inline-edit forms (create/edit/delete node, edit/delete edge), matching the precedent already set for the Sync button — a real UX gap, not a broken feature, root-caused via a long Playwright investigation into what looked at first like a completely dead Create button. `.gitignore` also gained `manual-undo/` (the same recurring class of gap hit for every prior new data store in this project).

**Phase 13 (graph editor remediation, round 2 hands-on feedback) is done** — tasks 13.1–13.4 built and tested per `plans/phase-13-tasks.md`, no new persona design round (the project owner's own feedback was concrete enough to build from directly, matching prior remediation rounds' discipline). **Task 13.1**, the most architecturally significant: manual node/edge create/edit/delete (`wf-mcp-server/lib/manual-edit-ops.mjs`) no longer independently pay the ~7s live-Foundry-poll cost every write used to — a new `applyManualMutations` appends each write to the world's one ongoing "manual-edit" `review-state.mjs` batch (auto-accepted at creation via `rollback.mjs`'s own `acceptMutations`, real pre-state capture as a side benefit) and applies HEADLESSLY RIGHT AWAY for near-instant feedback. Pushing to a live Foundry client is now the EXISTING, completely unmodified Sync button/route (`syncOp`), reused rather than reinvented — proven for real, not assumed, by a background-watcher-simulated test showing a later sync genuinely writes to and polls the real `world-fabric-mutations.json` bridge file. Undo's own inverse deliberately routes through the SAME headless-immediate mechanism (not the old dual-path apply) specifically to avoid a real correctness risk: an independent live-bridge write for undo could leave the original still-unsynced mutation un-removed from the batch, so a later sync would resurrect exactly what was just undone. All six Phase 12 undo action types re-verified working; review-ui gained a standalone Graph view sync bar mirroring the Review screen's existing pattern. **Task 13.2**: the graph layout's previously-fixed 960×620 box now scales both dimensions with `sqrt(nodeCount/8)` (keeping per-node spacing roughly constant as a graph grows, with a safeguard against ever shrinking below already-cached node positions), coordinate math now reads the rendered SVG's own live viewBox instead of a fixed constant, zoom scales CSS width and height together off the current dims (previously width tracked the container at 100%, silently defeating a bigger viewBox's whole point), and real click-and-drag panning was added for the standalone Graph view only. A genuine 18-entity/20-edge fixture confirmed the reported overlap (min pairwise node distance ~44 layout units, visibly touching nodes) before this change and confirmed the fix after (60–100+ units, visibly separated) via real before/after screenshots; popover anchoring, zoom, and batch-mode rubber-band selection + popover Accept were all separately re-verified correct against the new per-render dims. **Task 13.3**: a new `redirectMentionScanRowToExistingOp` converts a still-pending "propose new" mention-scan row in place into a genuine link to a GM-chosen existing entity (removing the sibling would-be-new-entity edge entirely, leaving exactly one real link mutation, not a modified create), gated to propose-new rows only; review-ui gained a "Link to existing instead" button opening an inline entity picker reusing the standalone Graph view's own search rather than a third search UI. Verified end to end (not just UI-deep): a unit test confirms accepting afterward creates the edge, not a duplicate; real headless Chromium confirmed the row's badge genuinely flips from "New" to "Existing". **Task 13.4**: a new, purely deterministic pre-pass in `graph-import/scan-mentions.mjs` (stopword-stripped token overlap + single-token edit-distance ratio, combined into one conservative `nameSimilarity` score) runs alongside the existing exact-match dedup, rewriting a near-miss mention's name to the matched entity's own exact stored name so it resolves through the SAME classify-and-shape path a real exact match would — confirmed via dedicated tests that a deliberately-constructed near-miss ("Gorrim Smith" vs "Gorrim the Smith") is caught while two genuinely different similarly-named entities ("Kael" vs "Kaelen") are correctly NOT merged, and the existing real-API scan-mentions smoke test still passes unmodified. A self-review remediation pass found and fixed two real gaps in task 13.3: no validation that a redirect's target id actually exists in the live graph (fixed, matching `addEdgeOp`'s own established convention), and the redirected row's displayed type reflected the stale original mention's guess rather than the real target entity's own type (fixed). One noted, deliberate limitation (not a regression): task 13.2's overlap fix takes full effect for any graph laid out fresh or as new nodes are added, but an already-cached real graph's existing positions won't retroactively reflow, since the established "never rearranges across a reload" guarantee is deliberately preserved. Full root (24 suites) / `wf-mcp-server` (9 suites) / `review-ui` (72 tests) test suites pass throughout. See `plans/phase-13-tasks.md` and `PLAN.md`'s Phase 13 row for the full build summary. A persona-driven QA pass (DM personas running concrete test tasks against the real tool) follows next, per the project owner's own explicit request — not part of Phase 13's own execution.

**The QA pass ran — two DM personas actually drove the tool in a browser against real, isolated scratch worlds and found nine real, independently-confirmed issues** (not suspicions — each verified against real data/code/measured behavior). Two critical: **rollback can silently report false success** when a batch has two mutations touching the same field (root-caused precisely: restores apply in original chronological order, and `headless-apply.mjs`'s shallow merge lets a later, less-historical preState overwrite an earlier one — the exact scenario Phase 13.1's manual-edit auto-batching produces by design, an edit then its own later undo on the same field); and **a brand-new world is a dead end** (`bootstrapSnapshot()` exists and is tested but is never called from any production code path — no "create world" UI affordance anywhere blocks the entire bring-your-own-world flow for a genuinely fresh campaign). Plus seven more real findings: deferred-debt neighbor entries silently vanishing via a shared-`regionId` bug in resolve-pending's fan-out; developing/accepting a node's content not clearing its unreviewed flag (`prep-content-ops.mjs` never calls human-review tracking, unlike every other write path); a node-edit popover that can render off-screen near the bottom of the canvas; the Long-Unreviewed list showing raw ids instead of names (found independently by BOTH personas); "Narrate This" unreachable from the standalone entity page; and a duplicate-batch risk from an uncancelled in-flight scan request. Everything else held up well across both runs — extraction quality, the time-skip→deferred-debt→resolve-on-demand pipeline (called "the actual killer feature" by one persona), manual-edit speed (confirmed fixed from the earlier "too slow" complaint — sub-second to ~1.5s now), narration quality, and the graph view's zoom/pan/legibility.

**Phase 14 (QA pass remediation) is done** — all eight tasks (14.1–14.8) built and tested per `plans/phase-14-tasks.md`, in severity order, no new design round needed (each finding already stated a confirmed root cause and fix direction). **Task 14.1**, the most important correctness fix in this phase: `rollback.mjs`'s `rollbackBatch` built its restore-mutation array in original chronological order, and since `headless-apply.mjs`'s shallow merge means multiple restores targeting the same entity/edge id apply in array order with the LAST one winning, a batch with two accepted mutations on the same field left the later (less-historical) preState winning while still reporting full rollback success. Fixed by reversing the iteration order — a small, surgical change. A regression test drives the REAL `applyHeadless`/`bootstrapSnapshot` path (not a stand-in), confirmed to fail against the pre-fix order and pass with the fix, with every existing single-mutation rollback test (the common case) unchanged. **Task 14.2**: `bootstrapSnapshot()` (already built, already tested) was never wired into any production path — added `POST /api/worlds` plus a minimal "Create New World" card in Settings, verified via a route test starting from genuinely nothing on disk and real headless-Chromium verification of the full New Import → Review → Accept → Sync cycle against a brand-new world. **Task 14.3**: `resolvePending`'s fan-out recorded every folded-in neighbor as "resolved" regardless of whether it actually received a mutation, so a genuinely-untouched neighbor's real backlog silently vanished the moment any other mutation in the batch was accepted (the same shared-`regionId` mechanism `applyLedgerOutcome` uses to clear entries) — fixed by only recording/locking entries for entities the batch's mutations genuinely target, proven by a regression test reproducing the exact leak plus a companion test confirming genuinely-mutated entities still resolve correctly. Flagged, not fixed (different file, out of this task's scope): `time-skip/run-cycle.mjs`'s growth-bound sweep has the identical root cause in its own "bloated" entity path. **Task 14.4**: `prep-content-ops.mjs`'s accept path never called `markHumanReviewed`, unlike every other write path in the project — fixed with a one-line addition, proven by a real MCP-subprocess test asserting the underlying review record directly (not just its absence from a flagged list, which would be a false-positive-prone check). **Task 14.5**: the graph popover's positioning had no awareness of the container's visible viewport — reproduced via real bounding-box measurement (a 40-node fixture) showing a bottom-row node's edit form extending past the visible bounds with no way to discover it (mouse-wheel is captured for zoom, not scroll); fixed with a new `clampPopoverIntoView` shifting the popover by exactly its measured overflow, confirmed via before/after screenshots and a real Playwright click on Save succeeding post-fix. **Task 14.6**: `GET /api/unreviewed-entities` now includes each flagged entity's real name (mirroring the Deferred Debt tab's own lookup convention) instead of the raw id both QA personas independently flagged. **Task 14.7**: added a "Narrate This" affordance to the standalone entity page by finding the most recent batch with a genuinely-accepted mutation for that entity and reusing the existing `narrateEntity()` call exactly as the batch-scoped path does, with a typed, non-silent error (mapped to a clean 404) when nothing is narratable yet — verified with a real-API test proving the success path and real headless-Chromium verification of both cases. **Task 14.8**: fixed both halves of the scan-mentions duplicate-batch risk — client-side, a shared `AbortController` cancels any in-flight scan on navigation; server-side, a lightweight in-memory `dedupedScan` (keyed on world/entity/text, never caching a failure) reuses an in-flight-or-recently-settled result instead of starting a second one — proven by five deterministic unit tests, a real-API test firing two genuinely simultaneous requests, and real headless-Chromium verification reproducing the exact reported flow end to end. A self-review remediation pass re-confirmed both explicitly-flagged regression risks (14.1 against the original single-mutation rollback tests, 14.3 against a normal no-fan-out resolve-pending call) and found and fixed one real gap: `dedupedScan`'s cache only evicted a failed scan, leaving successfully-settled entries to grow unboundedly over a long server session — bounded to roughly one dedupe-window's worth of history. No new persisted data store was introduced (the scan-dedupe cache is pure in-memory), so no `.gitignore` gap this phase. Full root (24 suites) / `wf-mcp-server` (10 suites) / `review-ui` (88 tests) suites pass throughout, alongside four independently-run real-API smoke/live tests. See `plans/phase-14-tasks.md` and `PLAN.md`'s Phase 14 row for the full build summary.

**A frontend-architecture and a backend/security tradeoff study both ran after Phase 14, at the project owner's request** (a real "should we modernize the stack" question, given they're open to more complexity if it genuinely buys polish/speed/reduced friction). Both concluded **don't change the stack** — a component framework wouldn't fix the DOM propagation-ordering bug class this project has repeatedly hit (that's native event-bubbling semantics, identical under React/Svelte), and neither a real job queue nor SQLite solves a problem this single-process, single-user tool actually has. What they did recommend, concretely: a real committed browser-regression suite (there's never been one — every "verified in a browser" claim across 14 phases has been a one-off session), real mobile/responsive fixes (never designed for or tested against touch before), and — separately, safe remote access via a mesh VPN (Tailscale), explicitly **not** Foundry VTT's own port-forward-plus-password model, since this tool's worst case (a live-spend API key, direct write access to campaign data) is categorically worse than Foundry's own (someone sees your game).

**The security review found a real, confirmed, already-fixed vulnerability, not a hypothetical**: `review-ui/server.mjs` has zero authentication on any route, and all ~30 routes passed a client-supplied `dataDir`/`world` value straight into `join()`-based file paths across all seven of this project's flat-JSON stores — `node:path`'s `join()` doesn't stop `..` traversal, so this was a real unauthenticated arbitrary-file-read/write primitive, not just "read/write your campaign data." Fixed immediately (not deferred to a phase): `review-ui/server.mjs` no longer forwards a client-supplied `dataDir` to `resolveDir()` at any of its ~30 call sites (always resolves from `WF_DATA_DIR`/OS-default now — removing the untrusted input entirely rather than trying to validate an arbitrary path string), and `wf-mcp-server/lib/resolve.mjs`'s `resolveWorld()` now validates the world-id format centrally (covering both the HTTP and MCP callers). `wf-mcp-server`'s own MCP tool surface still accepts an explicit `dataDir` override where needed — that surface is local/trusted, spawned directly by a Claude Code session over stdio, never network-facing. New permanent regression tests at both the unit level (`wf-mcp-server/test/resolve.test.mjs`) and the route level (`review-ui/test/routes.test.mjs`) prove a malicious `dataDir` is silently ignored and a malicious `world` id is cleanly rejected. Full suite (24/11/90) confirmed passing throughout.

**Phase 15 (frontend regression suite + mobile responsive fixes) is ready to execute, not yet built** — the two concrete recommendations from the frontend review. See `plans/phase-15-tasks.md`.

Engineering conventions for writing code in this repo live in `.claude/skills/gm-tools-conventions/` — load it before touching `mutation-engine/`, `time-skip/`, `graph-import/`, or `wf-mcp-server/`.

## Key architectural fact to internalize immediately

`GraphService`, World Fabric's graph store, only runs **inside the Foundry browser session** — it depends on `game.settings`. Nothing outside Foundry (this stack included) can call it directly. World Fabric bridges this with files in the world's data folder:

- `world-fabric-snapshot.json` — auto-exported on every graph write, readable by anything
- `world-fabric-mutations.json` — write here, the in-Foundry mutation watcher polls every 5s, applies, clears to `[]`

Every tool in this stack that touches the graph (the MCP server, and the session runner once built) goes through this file bridge, not through Foundry's API. **Mutations only apply while a Foundry client has the target world open.**

## Where things are

| Path | What |
|------|------|
| `PLAN.md` | The durable plan — architecture, all 5 tools, build sequence with live status, maps workflow, test list |
| `wf-mcp-server/` | Built. MCP server exposing the graph (query + mutate) as Claude tools. See its own `README.md` for the tool list and config. Registered in `~/.mcp.json` as `world-fabric`. |
| `session-runner/` | Not built yet. Next up per the build sequence. |
| `/home/russell/foundry_worldFabric` | The World Fabric module itself (separate project, own memory/history — entity/edge schema, GraphService, cockpit UI). Read its `CLAUDE.md`/memory if you need module internals, not just the bridge. |
| `/home/russell/campaign` | The pinned Threadbare City / Merrath one-shot — a *consumer* of this stack, not part of it. Has its own hand-authored `dm-engine/` (a scripted precursor to the generalized session runner). **Different track** — don't pull one-shot-specific content work into GM_Tools sessions unless explicitly asked. |

## Config already in place

`~/.mcp.json` has both `foundryvtt` (generic actor/scene MCP, pending approval) and `world-fabric` (this stack's MCP server, `WF_DATA_DIR=/home/russell/foundrydata/Data`).
