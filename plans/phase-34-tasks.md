# GM_Tools — Phase 34 Task Plan: Foundation — adopt the full designer app, Shell v2 + Connection Menu, delta-fixes, the front door

**Status:** approved, ready to execute. Design record (roadmap 34–37, decisions, persona adoptions, grounding): `/home/russell/.claude/plans/ok-i-m-back-with-dazzling-newt.md` — **read it first.** The NEW handoff zip (`/opt/dev/GM_Tools/TTRPG Session Planning Tool.zip`) is the design source; 34.0 extracts it over `design/session-planner/` as the final design doc. This file is the execution checklist.

**Prerequisite reading:** `CLAUDE.md` → `PLAN.md` (Phase 34 row) → the design record → the new handoff README (`unzip -p` until 34.0 lands, then `design/session-planner/README.md`) → `.claude/skills/gm-tools-conventions/SKILL.md` → `.claude/skills/gm-tools-verification/SKILL.md`.

## Settled decisions (do not relitigate — Russell 2026-08-07/08 + persona adoptions)
- **Remove-from-graph = HYBRID**: design's inline two-click arm + children reparent UP one level; armed state expands into a consequence line ("reparents K inside · drops M links · used in N scenes") + the opt-in remove-from-all-scenes; collapses to bare "remove — sure?" when all counts are zero. Undo restores node+edges (not the opt-in scene cleanup — say so).
- **Sequencing**: Foundry loop first (35 Library → 36 ready-to-run push → 37 Chronicle).
- **Legacy views: keep by-hash, retire-as-replaced.** This phase retires `#settings` (→ Connection Menu panel) and `#import` (→ lore intake) with hash-redirects to the new homes.
- Chip has **3 states** (live / stale-by-index-age / off) + a **distinct sync-error badge**; "chip carries state, panel carries why". Launcher is single-instance (health-check first); first-run with no worlds lands on Create-World; missing `ANTHROPIC_API_KEY` never gates launch. Primary nav must stay visually distinct from in-page Prep|Run / Page|Cards controls. Panel: Esc + click-outside dismiss.
- Stale-README corrections (34.0 documents; do NOT regress code to match stale text): `scene-membership` retired (Phase 33); `SceneElement.stat` exists (Phase 29); the push path is the Phase-32 file bridge; README §F is stale where the new prototype markup differs (Files-note + markup win).

## Pre-specified route/store contract (orchestrator-locked so 34.0 ∥ 34.1 can run in parallel; both implement THIS, not their own guesses)
- `GET /api/foundry/connection?world=` → `{state:"live"|"stale"|"off", exportedAt, ageMs, staleThresholdMs, counts:{actors,items,scenes,journals}|null, lastSync:{at,ok,error?}|null, world}`. Derivation: `off` = no readable foundry-index; `stale` = index `exportedAt` older than threshold (default 15 min, overridable via settings); else `live`. `counts` from the index arrays. `lastSync` from a small per-world sync-log the sync route writes.
- `POST /api/foundry/sync-now {world}` → `{pulled:{bestiaryProposed,partyProposed,alreadyLinked}, indexAgeMs, state}` — composition: read index (do NOT block on Foundry being up; if index missing → `{state:"off"}` with a clear message) → `pullFoundryActorsToStores` → append the sync-log entry. (Reindex triggering stays Foundry-side via `api.reindexForGmTools()`; the route documents that.)
- Settings store `session-planner/app-settings.mjs` (per-world JSON, env `GM_TOOLS_APP_SETTINGS_DIR`, SCHEMA_VERSION): `{campaignName, gameSystem, calendar, proseModel, imageModel, staleThresholdMs}` — all optional strings/nums. Routes `GET/POST /api/settings?world=` (POST = patch). Only `campaignName` + `calendar` + `staleThresholdMs` are WIRED this phase; model rows stored-not-wired (flagged in UI copy).
- `POST /api/lore/worldanvil {world, url}` → server-side fetch of the URL (plain `fetch`, text/html → strip to text, cap at `MAX_WRITEUP_CHARS`) → delegate to `importWriteup` → same `{batchId, mutationCount, importSummary, headline}` shape as `/api/writeup-propose`. A paste route ALREADY exists (`/api/writeup-propose`) — the panel uses it for paste mode.
- `POST /api/graph/nodes/:entityId/remove-reparent-up {world}` → the 34.3 hybrid delete: reparent every containment-child to the node's parent (or unparent if none) THEN delete the node cascading its remaining edges — ONE atomic undo slot → `{entityId, reparentedChildren, droppedEdges}`. (Composes `reparentNode` + `deleteNodeOp` internals in `manual-edit-ops.mjs`.) The existing `remove-from-scenes` route is reused unchanged for the opt-in.

## Tasks
- **34.0** (Sonnet) — extract handoff → `design/session-planner/` (replace the 2 old prototypes; add `Chronicle.dc.html`, `Library.dc.html`, `Connection Menu.dc.html`; exclude `support.js`; keep zip gitignored). Write `plans/phase-34-adoption.md` (delta table D1–D12 + SAME items, stale-README corrections, persona adoptions, the locked hybrid spec). Author `phase34-fixture.mjs` + `phase34-*.e2e.mjs` red contract: 4-way nav + active states; chip renders with state from `GET /api/foundry/connection` (mock/expect-off); panel skeleton (open/dismiss/sections present); `#settings`/`#import` redirect; D4 named prev/next; hybrid remove-from-graph (arm → consequence line → reparent-up asserted via graph GET); cosmetics presence-level. Confirmed red for the right reasons; existing suites stay green.
- **34.1** (Sonnet) — implement the pre-specified contract above: connection route (+ sync-log), sync-now, settings store/routes, worldanvil route, `remove-reparent-up` op+route. Deterministic tests for every op (incl. reparent-up: children adopt grandparent; root-child case; atomic undo restores original topology). No frontend.
- **34.2** (Opus) — Shell v2: 4-way nav per prototype (distinct from in-page toggles), `#chronicle`/`#library` scaffold pages (designed placeholders: correct chrome + "arrives in Phase 35/37" copy), the Connection Menu component (chip 3-states + pending-review count + error badge; 560px panel: Foundry section w/ world-switch + counts + Sync now; lore intake paste + World Anvil w/ rollup + "Review" handoff → `#review/<batchId>` until Phase 37; history over `GET /api/batches`; settings incl. stored-not-wired model rows). Fold `#settings` + `#import` (redirect hashes; retire their nav-less views). 34.0's shell/chip/panel e2e green.
- **34.3** (Sonnet, + Haiku for D9–D12) — D4 named prev/next with Start/End-of-plan ends; D5–D8 hybrid remove-from-graph frontend (arm → consequence line + opt-in; wire to `remove-reparent-up`); D9–D12 cosmetics (icon-circle type chips, 220px search, tooltips, tray hint, "all 1 scene" grammar). Haiku output is flagged-advisory — Sonnet/orchestrator reviews the diff. 34.0's delta e2e green.
- **34.4** (Sonnet) — `bin/gm-tools` launcher (node): health-check `GET /api/worlds` on 8787 → if ours, open browser only; else spawn `review-ui` server w/ `../.env`, wait for listen, open `http://localhost:8787/`; port stays fixed; clear error if 8787 is foreign. Root `package.json` gains `"app": "node bin/gm-tools"`; a `gm-tools.desktop` entry; no-worlds first-run → land on Create-World then persist default; missing-API-key one-line notice. Short RUN.md.
- **34.5** (orchestrator) — independent re-verification (all suites), screenshot gallery (shell/nav/chip/panel/deltas/launcher flow), PLAN.md flip, hand off to Russell's browser pass **launched via the launcher**.

## Dependency / tiering / concurrency
| Task | Tier | Deps |
|---|---|---|
| 34.0 adoption + QE | Sonnet | — |
| 34.1 backend glue | Sonnet | — (parallel with 34.0; both implement the pre-specified contract) |
| 34.2 shell + ConnMenu | Opus | 34.0, 34.1 |
| 34.3 delta-fixes | Sonnet+Haiku | 34.0, 34.1 (remove-reparent-up) |
| 34.4 front door | Sonnet | — (disjoint files; anytime) |
| 34.5 verify + pass | orch | all |
34.2 and 34.3 share `world-view.js`/shell files → sequential (34.2 then 34.3). 34.4 is disjoint (`bin/`, root package.json) → parallel-safe. `git add` explicit paths; commit per task; orchestrator independently re-runs root + wf-mcp + review-ui deterministic + full e2e per wave (lone untouched-file 30s timeout = suspected parallel-load flake → isolation re-run). `foundry_worldFabric` untouched this phase (baseline HEAD `da0f249` + pre-existing tree).

## Verification
QE-first phase34 e2e red→green; existing suites stay green (root 65/66 known `snowball-delta`, wf-mcp 15/15, review-ui deterministic ~173+, e2e 79/79 + new). Grep-clean on retired `#settings`/`#import` views. **Final gate: Russell's hands-on pass, launched via `npm run app` / `bin/gm-tools` only.**
