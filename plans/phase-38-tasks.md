# GM_Tools — Phase 38 Task Plan: Content pipeline II + navigation

**Status:** approved (Russell 2026-08-10), ready to execute. Sequenced BEFORE Phase 37 (Chronicle) per his call. Design record: `/home/russell/.claude/plans/ok-i-m-back-with-dazzling-newt.md` (Phase 38 section — **read it first**, incl. both explorer groundings). This file is the execution checklist.

**Prerequisite reading:** `CLAUDE.md` → `PLAN.md` (Phase 36/36.4/38 rows) → the design record → `plans/phase-32-bridge-contract.md` (v2, becomes v3 in 38.0) → `.claude/skills/gm-tools-conventions/SKILL.md` → `.claude/skills/gm-tools-verification/SKILL.md` (module baseline `6d53d97`).

## Settled decisions (do not relitigate — Russell 2026-08-10)
- **Czepeku = the Patreon per-pack module route.** Russell supplies manifest URLs/zips from the gated Master Post at 38.4 (his login gates them). The installed "CZEPEKU Universe" module needs czepeku.com Premium (NOT Patreon) — leave installed, treat as inert, note it in the gallery.
- **Plutonium is explicitly user-requested** (personal use, his own world). The old "no Plutonium/SRD-only" rule was scoped to the agent-built 35.pre test bed and is superseded for HIS content by his direct request. Verified: manifest `https://github.com/TheGiddyLimit/plutonium-next/releases/latest/download/module.json`, v14-verified ("14.365") + dnd5e 5.3.x (ours: 14.363/5.3.3); needs lib-wrapper ≥1.13 (installed 1.13.5.1); conflicts only with better-rolltables (absent).
- **Loyalty tree edges = `{membership, fealty}`** (existing free-string vocab; both appear in fixtures + the writeup-import prompt). Drag-in-Loyalty-mode creates a `membership` edge. Ally/social stay ordinary links. Single-parent rule: most-recent edge wins; multi-loyalty = flagged future.
- **Spatial stays the default World view** — every phase30/31/33 containment/reparent pin must stay green untouched. `reparentNode` (containment-only, e2e-pinned) is NEVER repurposed — the loyalty re-anchor is a NEW sibling op.
- Plan deletion never touches scenes (plans hold sceneId POINTERS — grounded plans.mjs:66–75; copy: "its scenes are untouched and stay in the Scene library").
- 38.1 is the ONLY wave allowed to modify `/opt/dev/foundry_worldFabric` (sanctioned wave #3, atop `6d53d97`; move the verification-skill baseline in-wave).

## Grounding facts every task must respect (explorer reports 2026-08-10, cited in task outputs)
- **Bridge blind spots:** the index enumerates ONLY world actors/users/scenes/playlists (`exportFoundryIndex`, foundry-bridge.mjs:376–391). NO `game.items` (loose world Items invisible — where Plutonium world-imports land) and NO `game.packs` (ALL compendium content invisible — where Czepeku pack scenes live until imported).
- Czepeku packs: standalone modules; scenes in COMPENDIA; map files on disk under `Data/modules/<pack>/`; packs can be 100s of MB (fine — Foundry now lives on the 206G-free disk).
- Compendia are lazy: header enumeration must use `pack.index` (cheap), never full-document loads.
- Plan-delete backend EXISTS end-to-end: `deletePlan` (plans.mjs:212), `DELETE /api/scene-planning/plans/:id` (server.mjs:2367, idempotent), runsheet button + confirm (`planner-plan-delete-btn`/`confirmDeletePlan`, app-shell.js:663–718). ONLY the rail ✕ on `shell-plan-item` rows is missing — mirror the scene rows' ✕ + `toggleSceneDeleteConfirm` (app-shell.js:320–432) and the scene-delete e2e pattern (scene-delete.e2e.mjs:133–164).
- Loyalty: `buildDerived` hardcodes `relationshipType === "containment"` in ONE place (world-view.js:153–168) — parameterize the predicate; helpers (parentIdOf/childIdsOf/rootIds/ancestorChain) are already type-agnostic over the derived maps. `looseReasons`/loose-threads stay containment-based. Segmented-toggle idiom: `buildSegmentedControl` (session-planner-view.js:2891) — lift or mirror locally. `faction` is a first-class node type (glyph ⬗). Reparent toast copy is containment-flavored ("is now inside") — Loyalty needs its own copy family ("now serves …").

## Tasks
- **38.0 (Sonnet) — QE + bridge-contract v3 + the two UI contracts.** Amend `plans/phase-32-bridge-contract.md` to v3: index additions `worldItems[]` (reuse `extractItem` shape) + `compendia[]` = `[{packId, label, documentType, count, entries?}]` where `entries:[{id, name, thumb}]` is included for Scene-type packs only (via `pack.index`); NEW op `import_compendium_scene {packId, entryId}` → creates the world Scene from the compendium doc (creator uses the v14 idiom — the implementer verifies the exact API, e.g. `game.packs.get(packId).getDocument(entryId)` → `Scene.create(doc.toObject())` or the official import helper — and documents what v14 actually wants; background/Level-doc behavior must survive the import). Write `phase38-fixture.mjs` pinning: pull-mapper — `worldItems[]` → Reliquary proposals (`ownerFoundryActorUuid:null`, `ownerPartyMemberId:null`, sourceText "Pulled from Foundry world items"); compendium Scene entries → Stagecraft rows with additive `compendiumRef {packId, entryId}` + `foundryRef:null` until imported + meta "in compendium — import to stage"; the ACCEPT action on a compendiumRef row composes `import_compendium_scene` through the ops channel (reuse the 36.3 pending/reconcile machinery — pin the flow: accept → op → result sceneUuid → row gains `foundryRef.sceneUuid` + normal accepted-map behavior; next pull refreshes imagePath/dims); dedup rule for re-pulls (compendium entries upsert on packId+entryId; an imported entry's browse row and its world-scene row must NOT duplicate — pin the reconciliation). Plan-delete rail ✕ contract (testids mirroring the scene family: `shell-plan-item-delete-btn`, confirm panel, copy). Loyalty contract: `Spatial|Loyalty` toggle testids in `wv-tree-head`; Loyalty derived tree over `{membership, fealty}` (single-parent most-recent-wins); NEW op `anchorMembership(entityId, parentId)` + route `POST /api/graph/nodes/:id/anchor-membership {world, parentId}` (removes the entity's existing membership/fealty PARENT edges, adds ONE `membership` edge, atomic undo restoring priors, cycle guard — mirror reparentNode's shape, reparentNode untouched); drag semantics in Loyalty mode; Spatial default + all existing pins green. Red e2e (route 404s, toggle/tree/✕ selector absence, ops-file shape for the import op) + fixture mock index with worldItems + a Scene-pack compendium.
- **38.1 (Opus — foundry_worldFabric, sanctioned wave #3).** Atop `6d53d97` (+ the same pre-existing 4-item tree, untouched): `exportFoundryIndex` gains `game.items` → `worldItems[]` and `game.packs` → `compendia[]` per contract v3 (`pack.index` reads only; Scene packs get `{id,name,thumb}` entries; non-Scene packs headers only); the watcher's creator fan-out gains `import_compendium_scene` (at-most-once guards already cover it); pure-fn tests vs the shared phase38 fixture shapes; existing module tests stay green (27+); update the live-smoke doc; then the GM_Tools verification-skill baseline commit (LAST, explicit single-file add, rebase-retry on race).
- **38.2 (Sonnet) — GM_Tools content plumbing + Library surfaces.** Implements the 38.0 contract: pull-mapper growth (`worldItems`→Reliquary unowned proposals; `compendia` Scene entries→Stagecraft browse rows w/ upsert-on-packId+entryId + imported-row reconciliation); the import-on-accept composition (ops channel + pending/reconcile reuse — a browse-row accept is asynchronous like a staged push: pending until the result lands, then the row completes); Reliquary renders unowned rows cleanly (no owner link line) + a quiet "world item" provenance; Stagecraft browse rows render the "in compendium — import to stage" meta + thumb when present; counts/search absorb Plutonium-scale row counts (flag if the shelf strains — no redesign this phase). Deterministic tests + the 38.0 e2e flipped green for this slice.
- **38.3 (Sonnet) — plan-delete ✕ + the Loyalty tree.** Rail ✕ per contract (mirror `fillRailScenes`; reuse the EXISTING delete route; e2e mirroring scene-delete's rail test + a route-level idempotency/scenes-survive test via a new `deletePlanViaRoute` helper). Loyalty: parameterize `buildDerived`'s predicate; the toggle (Spatial default); Loyalty rendering (factions as branches; nodes with no loyalty parent = roots); drag → `anchorMembership` route + optimistic reload + undo toast ("now serves X" family); `anchorMembership` op in `manual-edit-ops.mjs` with deterministic tests (prior-edge removal incl. fealty, cycle guard, atomic undo, root-anchor case). Full e2e green incl. phase38.
- **38.4 (orchestrator + Russell) — installs + live acceptance + gallery.** Orchestrator installs Plutonium headlessly (manifest above → `Data/modules/plutonium/`); **Russell supplies the Czepeku pack manifests/zips** (installed the same way); ONE Foundry server restart (Russell's terminal kill, established pattern) + enable modules; then the live proof: fresh pull → Stagecraft lists the Czepeku pack scenes as browsable rows with thumbs → import 2–3 → world scenes with REAL map art → stage one → Foundry ready-to-run with a real map; Plutonium world-import a spread of items → fresh pull → Reliquary jumps, searchable. Gallery + PLAN.md flip. **Final gate: Russell's pass.**

## Dependency / tiering / concurrency
| Task | Tier | Deps |
|---|---|---|
| 38.setup | orch | — |
| 38.0 contract | Sonnet | — |
| 38.1 module wave | Opus | 38.0 (hard) |
| 38.2 content plumbing | Sonnet | 38.0 (hard) — ∥ 38.1 (different repos) |
| 38.3 UI wishes | Sonnet | 38.2 (shared server.mjs/view files) |
| 38.4 installs + acceptance | orch + Russell | 38.1 + 38.3 + Russell's manifests |
`git add` explicit paths; commit per task; orchestrator independently re-runs all suites per wave (isolation re-run before calling a lone parallel-run e2e failure a regression). Module repo: HEAD expected to move past `6d53d97` in 38.1 ONLY.

## Verification
QE-first phase38 e2e red→green; existing suites stay green (baseline: e2e 156/156, review-ui det 218/218, root 71/72 known `snowball-delta`, wf-mcp 18/18, module 27/27). 38.4's live browse→import→stage with a REAL Czepeku map + the Plutonium Reliquary jump are the technical gates; **Russell's pass is the final gate.**
